# Python API reference

This reference is generated from the production Python abstract syntax tree.
It includes compatibility modules and every package module, class, method,
nested callable, parameter, return annotation, and source docstring.

Modules documented: **51**.

Definitions documented: **350**.

Do not edit this file independently of the source contracts. Regenerate it
after changing public or private APIs.

## `audio`

Source: [`audio.py`](../audio.py#L1)

```text
Backward-compatible imports for cinematic audio planning.
```

## `context_loader`

Source: [`context_loader.py`](../context_loader.py#L1)

```text
Backward-compatible imports for the reorganized context package.
```

## `prompt_compiler`

Source: [`prompt_compiler.py`](../prompt_compiler.py#L1)

```text
Backward-compatible imports for cinematic prompt compilation.
```

## `server`

Source: [`server.py`](../server.py#L1)

```text
Compatibility entry point for the Little Planet Wonders MCP server.
```

## `lpw`

Source: [`src/lpw/__init__.py`](../src/lpw/__init__.py#L1)

```text
Context-aware cinematic shot planning tools.
```

## `lpw.audio`

Source: [`src/lpw/audio/__init__.py`](../src/lpw/audio/__init__.py#L1)

```text
Expose the public audio package API for LPW.
```

## `lpw.audio.compiler`

Source: [`src/lpw/audio/compiler.py`](../src/lpw/audio/compiler.py#L1)

```text
Provide compiler services for the LPW cinematic pipeline.
```

### `resolved_audio_context`

Defined at line 14.

```python
def resolved_audio_context(project_id: str) -> dict[str, Any]
```

```text
Execute audio context.

Args:
    project_id (str): Stable identifier of the project whose context is used.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `resolved_voice_context`

Defined at line 30.

```python
def resolved_voice_context(project_id: str, character_id: str) -> dict[str, Any]
```

```text
Execute voice context.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    character_id (str): Stable identifier of the character.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

### `compile_dialogue_package`

Defined at line 51.

```python
def compile_dialogue_package(request: DialogueRequest) -> dict[str, Any]
```

```text
Compile dialogue package.

Args:
    request (DialogueRequest): Typed request containing the inputs for this
        operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

## `lpw.audio.defaults`

Source: [`src/lpw/audio/defaults.py`](../src/lpw/audio/defaults.py#L1)

```text
Define immutable defaults and supported values for this package.
```

## `lpw.audio.music`

Source: [`src/lpw/audio/music.py`](../src/lpw/audio/music.py#L1)

```text
Provide music services for the LPW cinematic pipeline.
```

### `compile_music_cue_sheet`

Defined at line 15.

```python
def compile_music_cue_sheet(*, project_id: str, episode_id: str, scene_id: str, cues: list[dict[str, Any]], dialogue_present: bool, target_age: str = 'two-to-five') -> dict[str, Any]
```

```text
Compile reusable music cues while preserving dialogue and response pauses.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    episode_id (str): Stable identifier of the episode being produced.
    scene_id (str): Stable identifier of the scene being processed.
    cues (list[dict[str, Any]]): Structured audio or music cue definitions.
    dialogue_present (bool): Whether dialogue must have priority in the mix.
    target_age (str): Audience age range used by safety rules. Defaults to ``'two-
        to-five'``.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

## `lpw.audio.pipeline`

Source: [`src/lpw/audio/pipeline.py`](../src/lpw/audio/pipeline.py#L1)

```text
Provide pipeline services for the LPW cinematic pipeline.
```

### `_compile_music_instructions`

Defined at line 17.

```python
def _compile_music_instructions(character_id: str | None, emotional_tone: str, audio: dict[str, Any]) -> dict[str, Any]
```

```text
Compile music instructions.

Args:
    character_id (str | None): Stable identifier of the character.
    emotional_tone (str): Emotional tone used by this operation.
    audio (dict[str, Any]): Audio used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `_compile_ambience_instructions`

Defined at line 46.

```python
def _compile_ambience_instructions(location_id: str, audio: dict[str, Any]) -> dict[str, Any]
```

```text
Compile ambience instructions.

Args:
    location_id (str): Stable identifier of the location.
    audio (dict[str, Any]): Audio used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `_compile_sound_effects`

Defined at line 76.

```python
def _compile_sound_effects(audio: dict[str, Any]) -> list[dict[str, Any]]
```

```text
Compile sound effects.

Args:
    audio (dict[str, Any]): Audio used by this operation.

Returns:
    list[dict[str, Any]]: Result produced by the operation.
```

### `_compile_mixing_instructions`

Defined at line 93.

```python
def _compile_mixing_instructions(audio: dict[str, Any]) -> dict[str, Any]
```

```text
Compile mixing instructions.

Args:
    audio (dict[str, Any]): Audio used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `compile_cinematic_shot`

Defined at line 116.

```python
def compile_cinematic_shot(shot_request: ShotRequest, *, dialogue_request: DialogueRequest | None = None, dialogue_audio_path: str | None = None, sound_effects: list[dict[str, Any]] | None = None) -> dict[str, Any]
```

```text
Build the complete, ordered video/audio pipeline plan for one shot.

Args:
    shot_request (ShotRequest): Shot request used to compile the visual package.
    dialogue_request (DialogueRequest | None): Optional dialogue request paired with
        the shot. Defaults to ``None``.
    dialogue_audio_path (str | None): Path reserved for the clean dialogue audio.
        Defaults to ``None``.
    sound_effects (list[dict[str, Any]] | None): Sound effects used by this
        operation. Defaults to ``None``.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

## `lpw.audio.sound_effects`

Source: [`src/lpw/audio/sound_effects.py`](../src/lpw/audio/sound_effects.py#L1)

```text
Provide sound effects services for the LPW cinematic pipeline.
```

### `compile_sound_cue_sheet`

Defined at line 22.

```python
def compile_sound_cue_sheet(*, project_id: str, scene_id: str, shot_id: str, location_id: str, cues: list[dict[str, Any]], dialogue_present: bool, participation_pause: bool = False) -> dict[str, Any]
```

```text
Validate visible-action cues and compile a child-safe sound plan.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    location_id (str): Stable identifier of the location.
    cues (list[dict[str, Any]]): Structured audio or music cue definitions.
    dialogue_present (bool): Whether dialogue must have priority in the mix.
    participation_pause (bool): Whether the cue overlaps a child-response pause.
        Defaults to ``False``.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

## `lpw.audio.voice_production`

Source: [`src/lpw/audio/voice_production.py`](../src/lpw/audio/voice_production.py#L1)

```text
Provide voice production services for the LPW cinematic pipeline.
```

### `VoiceProductionService`

Defined at line 27.

```python
class VoiceProductionService
```

```text
Prepare, approve, and lock exact dialogue without generating audio.
```

#### `VoiceProductionService.__init__`

Defined at line 30.

```python
def __init__(self, state_root: Path | str | None = None) -> None
```

```text
Initialize the service with its configured dependencies.

Args:
    state_root (Path | str | None): Root directory used for generated runtime
        state. Defaults to ``None``.
```

#### `VoiceProductionService.prepare_exact_dialogue`

Defined at line 39.

```python
def prepare_exact_dialogue(self, *, project_id: str, episode_id: str, scene_id: str, shot_id: str, character_id: str, exact_dialogue: str, language: str, primary_emotion: str, ending_emotion: str | None = None, emotional_intensity: float = 0.4, speaking_speed: str = 'slow-to-moderate', important_words: list[str] | None = None, pronunciation_notes: list[str] | None = None, pause_instructions: list[str] | None = None, audience_response_pause_seconds: float = 0, gesture_guidance: str | None = None, facial_expression_guidance: str | None = None) -> dict[str, Any]
```

```text
Prepare exact dialogue.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    episode_id (str): Stable identifier of the episode being produced.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    character_id (str): Stable identifier of the character.
    exact_dialogue (str): Exact approved words that must be spoken once.
    language (str): Language used for dialogue and pronunciation rules.
    primary_emotion (str): Primary emotional direction for the performance.
    ending_emotion (str | None): Emotional direction at the end of the line.
        Defaults to ``None``.
    emotional_intensity (float): Child-safe normalized emotional intensity.
        Defaults to ``0.4``.
    speaking_speed (str): Speaking speed used by this operation. Defaults to
        ``'slow-to-moderate'``.
    important_words (list[str] | None): Important words used by this operation.
        Defaults to ``None``.
    pronunciation_notes (list[str] | None): Pronunciation notes used by this
        operation. Defaults to ``None``.
    pause_instructions (list[str] | None): Pause instructions used by this
        operation. Defaults to ``None``.
    audience_response_pause_seconds (float): Audience response pause seconds
        used by this operation. Defaults to ``0``.
    gesture_guidance (str | None): Gesture guidance used by this operation.
        Defaults to ``None``.
    facial_expression_guidance (str | None): Facial expression guidance used by
        this operation. Defaults to ``None``.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

#### `VoiceProductionService.lock_exact_dialogue`

Defined at line 167.

```python
def lock_exact_dialogue(self, *, project_id: str, episode_id: str, scene_id: str, shot_id: str, character_id: str, dialogue_version: int, audio_path: str, verified_transcript: str, reviewer: str, review: dict[str, bool]) -> dict[str, Any]
```

```text
Execute exact dialogue.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    episode_id (str): Stable identifier of the episode being produced.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    character_id (str): Stable identifier of the character.
    dialogue_version (int): Positive version number of the dialogue package.
    audio_path (str): Path to the audio asset being processed.
    verified_transcript (str): Reviewed transcript expected to match the
        dialogue exactly.
    reviewer (str): Human reviewer identity recorded with the decision.
    review (dict[str, bool]): Required review checks and their pass/fail values.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
    FileNotFoundError: If inputs, context, state, or provider output are invalid.
```

#### `VoiceProductionService.compile_lip_sync_context`

Defined at line 254.

```python
def compile_lip_sync_context(self, *, project_id: str, episode_id: str, scene_id: str, shot_id: str, character_id: str, dialogue_version: int) -> dict[str, Any]
```

```text
Compile lip sync context.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    episode_id (str): Stable identifier of the episode being produced.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    character_id (str): Stable identifier of the character.
    dialogue_version (int): Positive version number of the dialogue package.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `VoiceProductionService._select_reference`

Defined at line 324.

```python
def _select_reference(voice: dict[str, Any], emotion: str) -> str | None
```

```text
Select reference.

Args:
    voice (dict[str, Any]): Voice used by this operation.
    emotion (str): Requested emotional delivery.

Returns:
    str | None: Result produced by the operation.
```

#### `VoiceProductionService._next_version`

Defined at line 347.

```python
def _next_version(self, project_id: str, episode_id: str, scene_id: str, shot_id: str, character_id: str) -> int
```

```text
Execute version.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    episode_id (str): Stable identifier of the episode being produced.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    character_id (str): Stable identifier of the character.

Returns:
    int: Result produced by the operation.
```

#### `VoiceProductionService._dialogue_root`

Defined at line 377.

```python
def _dialogue_root(self, project_id: str, episode_id: str, scene_id: str, shot_id: str, character_id: str) -> Path
```

```text
Execute root.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    episode_id (str): Stable identifier of the episode being produced.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    character_id (str): Stable identifier of the character.

Returns:
    Path: Result produced by the operation.
```

#### `VoiceProductionService._dialogue_directory`

Defined at line 408.

```python
def _dialogue_directory(self, project_id: str, episode_id: str, scene_id: str, shot_id: str, character_id: str, version: int) -> Path
```

```text
Execute directory.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    episode_id (str): Stable identifier of the episode being produced.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    character_id (str): Stable identifier of the character.
    version (int): Positive version number of the stored artifact.

Returns:
    Path: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

#### `VoiceProductionService._identifiers`

Defined at line 440.

```python
def _identifiers(project_id: str, episode_id: str, scene_id: str, shot_id: str, character_id: str) -> tuple[str, str, str, str, str]
```

```text
Execute identifiers.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    episode_id (str): Stable identifier of the episode being produced.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    character_id (str): Stable identifier of the character.

Returns:
    tuple[str, str, str, str, str]: Result produced by the operation.
```

#### `VoiceProductionService._hash_file`

Defined at line 468.

```python
def _hash_file(path: Path) -> str
```

```text
Execute file.

Args:
    path (Path): Filesystem path read or written by the operation.

Returns:
    str: Result produced by the operation.
```

#### `VoiceProductionService._now_iso`

Defined at line 484.

```python
def _now_iso() -> str
```

```text
Execute iso.

Returns:
    str: Result produced by the operation.
```

## `lpw.config`

Source: [`src/lpw/config.py`](../src/lpw/config.py#L1)

```text
Provide config services for the LPW cinematic pipeline.
```

### `_configured_path`

Defined at line 13.

```python
def _configured_path(variable: str, default: Path) -> Path
```

```text
Execute path.

Args:
    variable (str): Variable used by this operation.
    default (Path): Default used by this operation.

Returns:
    Path: Result produced by the operation.
```

### `context_root`

Defined at line 26.

```python
def context_root() -> Path
```

```text
Return the read-only source-of-truth context directory.

Returns:
    Path: Result produced by the operation.
```

### `workflows_root`

Defined at line 41.

```python
def workflows_root() -> Path
```

```text
Execute root.

Returns:
    Path: Result produced by the operation.
```

### `renders_root`

Defined at line 50.

```python
def renders_root() -> Path
```

```text
Execute root.

Returns:
    Path: Result produced by the operation.
```

### `runtime_root`

Defined at line 59.

```python
def runtime_root() -> Path
```

```text
Return the writable runtime directory, outside the packaged context.

Returns:
    Path: Result produced by the operation.
```

## `lpw.context`

Source: [`src/lpw/context/__init__.py`](../src/lpw/context/__init__.py#L1)

```text
Expose the public context package API for LPW.
```

## `lpw.context.chaining`

Source: [`src/lpw/context/chaining.py`](../src/lpw/context/chaining.py#L1)

```text
Provide chaining services for the LPW cinematic pipeline.
```

### `ContextChainError`

Defined at line 18.

```python
class ContextChainError(InvalidContextDataError)
```

```text
Raised when a versioned context chain cannot be resolved safely.
```

### `ResolvedContext`

Defined at line 23.

```python
class ResolvedContext
```

```text
Represent ResolvedContext behavior in the LPW pipeline.

Attributes:
    context (dict[str, Any]): Stored context value.
    chain (list[dict[str, Any]]): Stored chain value.
    context_hash (str): Stored context hash value.
```

### `ContextChainResolver`

Defined at line 36.

```python
class ContextChainResolver
```

```text
Resolve pinned cinema:// inheritance without modifying source context.
```

#### `ContextChainResolver.__init__`

Defined at line 39.

```python
def __init__(self, source_root: Path | str | None = None, state_root: Path | str | None = None) -> None
```

```text
Initialize the service with its configured dependencies.

Args:
    source_root (Path | str | None): Root directory containing immutable source
        context. Defaults to ``None``.
    state_root (Path | str | None): Root directory used for generated runtime
        state. Defaults to ``None``.
```

#### `ContextChainResolver.resolve`

Defined at line 55.

```python
def resolve(self, uri: str) -> ResolvedContext
```

```text
Resolve resolve.

Args:
    uri (str): Uri used by this operation.

Returns:
    ResolvedContext: Result produced by the operation.
```

#### `ContextChainResolver.compile_document`

Defined at line 67.

```python
def compile_document(self, document: dict[str, Any], *, source_uri: str) -> ResolvedContext
```

```text
Compile document.

Args:
    document (dict[str, Any]): Structured document to validate, merge, or
        persist.
    source_uri (str): Pinned URI identifying the source context document.

Returns:
    ResolvedContext: Result produced by the operation.
```

#### `ContextChainResolver._resolve`

Defined at line 88.

```python
def _resolve(self, uri: str, *, stack: tuple[str, ...]) -> tuple[dict[str, Any], list[dict[str, Any]]]
```

```text
Resolve resolve.

Args:
    uri (str): Uri used by this operation.
    stack (tuple[str, ...]): Stack used by this operation.

Returns:
    tuple[dict[str, Any], list[dict[str, Any]]]: Result produced by the operation.

Raises:
    ContextChainError: If inputs, context, state, or provider output are invalid.
```

#### `ContextChainResolver._compile_document`

Defined at line 121.

```python
def _compile_document(self, document: dict[str, Any], *, source_uri: str, stack: tuple[str, ...]) -> tuple[dict[str, Any], list[dict[str, Any]]]
```

```text
Compile document.

Args:
    document (dict[str, Any]): Structured document to validate, merge, or
        persist.
    source_uri (str): Pinned URI identifying the source context document.
    stack (tuple[str, ...]): Stack used by this operation.

Returns:
    tuple[dict[str, Any], list[dict[str, Any]]]: Result produced by the operation.

Raises:
    ContextChainError: If inputs, context, state, or provider output are invalid.
```

#### `ContextChainResolver._merge`

Defined at line 199.

```python
def _merge(base: dict[str, Any], override: dict[str, Any]) -> dict[str, Any]
```

```text
Execute merge.

Args:
    base (dict[str, Any]): Base used by this operation.
    override (dict[str, Any]): Override used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `ContextChainResolver._merge_lists`

Defined at line 221.

```python
def _merge_lists(key: str, base: list[Any], override: list[Any]) -> list[Any]
```

```text
Execute lists.

Args:
    key (str): Key used by this operation.
    base (list[Any]): Base used by this operation.
    override (list[Any]): Override used by this operation.

Returns:
    list[Any]: Result produced by the operation.
```

#### `ContextChainResolver._path_for_uri`

Defined at line 250.

```python
def _path_for_uri(self, uri: str) -> Path
```

```text
Execute for uri.

Args:
    uri (str): Uri used by this operation.

Returns:
    Path: Result produced by the operation.

Raises:
    ContextChainError: If inputs, context, state, or provider output are invalid.
```

#### `ContextChainResolver._project_path`

Defined at line 280.

```python
def _project_path(self, parts: list[str], uri: str) -> Path
```

```text
Execute path.

Args:
    parts (list[str]): Parts used by this operation.
    uri (str): Uri used by this operation.

Returns:
    Path: Result produced by the operation.

Raises:
    ContextChainError: If inputs, context, state, or provider output are invalid.
```

#### `ContextChainResolver._runtime_end_state_path`

Defined at line 319.

```python
def _runtime_end_state_path(self, segment_id: str) -> Path
```

```text
Execute end state path.

Args:
    segment_id (str): Stable identifier of the chained video segment.

Returns:
    Path: Result produced by the operation.

Raises:
    ContextChainError: If inputs, context, state, or provider output are invalid.
```

#### `ContextChainResolver._parse_pinned_uri`

Defined at line 345.

```python
def _parse_pinned_uri(uri: str) -> tuple[str, int]
```

```text
Parse pinned uri.

Args:
    uri (str): Uri used by this operation.

Returns:
    tuple[str, int]: Result produced by the operation.

Raises:
    ContextChainError: If inputs, context, state, or provider output are invalid.
```

#### `ContextChainResolver._string_list`

Defined at line 365.

```python
def _string_list(value: Any, field: str) -> list[str]
```

```text
Execute list.

Args:
    value (Any): Value inspected or transformed by the helper.
    field (str): Field used by this operation.

Returns:
    list[str]: Result produced by the operation.

Raises:
    ContextChainError: If inputs, context, state, or provider output are invalid.
```

#### `ContextChainResolver._immutable_paths`

Defined at line 387.

```python
def _immutable_paths(*contexts: dict[str, Any]) -> list[str]
```

```text
Execute paths.

Args:
    *contexts (dict[str, Any]): Contexts used by this operation.

Returns:
    list[str]: Result produced by the operation.
```

#### `ContextChainResolver._reject_immutable_changes`

Defined at line 404.

```python
def _reject_immutable_changes(self, base: dict[str, Any], override: dict[str, Any], immutable_paths: Iterable[str], source_uri: str) -> None
```

```text
Reject immutable changes.

Args:
    base (dict[str, Any]): Base used by this operation.
    override (dict[str, Any]): Override used by this operation.
    immutable_paths (Iterable[str]): Immutable paths used by this operation.
    source_uri (str): Pinned URI identifying the source context document.

Raises:
    ContextChainError: If inputs, context, state, or provider output are invalid.
```

#### `ContextChainResolver._apply_patch`

Defined at line 430.

```python
def _apply_patch(self, document: dict[str, Any], patch: Any, immutable_paths: Iterable[str]) -> None
```

```text
Apply patch.

Args:
    document (dict[str, Any]): Structured document to validate, merge, or
        persist.
    patch (Any): Patch used by this operation.
    immutable_paths (Iterable[str]): Immutable paths used by this operation.

Raises:
    ContextChainError: If inputs, context, state, or provider output are invalid.
```

#### `ContextChainResolver._get_dotted`

Defined at line 482.

```python
def _get_dotted(document: dict[str, Any], path: str) -> Any
```

```text
Return dotted.

Args:
    document (dict[str, Any]): Structured document to validate, merge, or
        persist.
    path (str): Filesystem path read or written by the operation.

Returns:
    Any: Result produced by the operation.
```

#### `ContextChainResolver._deduplicate_chain`

Defined at line 501.

```python
def _deduplicate_chain(chain: list[dict[str, Any]]) -> list[dict[str, Any]]
```

```text
Execute chain.

Args:
    chain (list[dict[str, Any]]): Chain used by this operation.

Returns:
    list[dict[str, Any]]: Result produced by the operation.
```

## `lpw.context.compiler`

Source: [`src/lpw/context/compiler.py`](../src/lpw/context/compiler.py#L1)

```text
Provide compiler services for the LPW cinematic pipeline.
```

### `ContextCompilerError`

Defined at line 40.

```python
class ContextCompilerError(Exception)
```

```text
Raised when story, scene, or shot context cannot be compiled.
```

### `ContextCompiler`

Defined at line 44.

```python
class ContextCompiler
```

```text
Load hierarchical YAML context and compile complete scene packages.
```

#### `ContextCompiler.__init__`

Defined at line 47.

```python
def __init__(self, context_root: Path | str = DEFAULT_CONTEXT_ROOT) -> None
```

```text
Initialize the service with its configured dependencies.

Args:
    context_root (Path | str): Root directory containing source context files.
        Defaults to ``DEFAULT_CONTEXT_ROOT``.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler.context_root`

Defined at line 68.

```python
def context_root(self) -> Path
```

```text
Execute root.

Returns:
    Path: Result produced by the operation.
```

#### `ContextCompiler.compile_scene`

Defined at line 76.

```python
def compile_scene(self, story_id: str, scene_id: str) -> dict[str, Any]
```

```text
Compile scene.

Args:
    story_id (str): Stable identifier of the story to load or compile.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `ContextCompiler.load_project`

Defined at line 118.

```python
def load_project(self) -> dict[str, Any]
```

```text
Load project.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `ContextCompiler.load_story`

Defined at line 133.

```python
def load_story(self, story_id: str) -> dict[str, Any]
```

```text
Load story.

Args:
    story_id (str): Stable identifier of the story to load or compile.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler.load_scene`

Defined at line 164.

```python
def load_scene(self, scene_id: str) -> dict[str, Any]
```

```text
Load scene.

Args:
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler.load_shot`

Defined at line 210.

```python
def load_shot(self, scene_id: str, shot_file_name: str) -> dict[str, Any]
```

```text
Load shot.

Args:
    scene_id (str): Stable identifier of the scene being processed.
    shot_file_name (str): Shot file name used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler.load_prop`

Defined at line 234.

```python
def load_prop(self, prop_id: str) -> dict[str, Any]
```

```text
Load prop.

Args:
    prop_id (str): Prop id used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler.load_wardrobe`

Defined at line 272.

```python
def load_wardrobe(self, wardrobe_id: str) -> dict[str, Any]
```

```text
Load wardrobe.

Args:
    wardrobe_id (str): Wardrobe id used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler.load_scene_state`

Defined at line 301.

```python
def load_scene_state(self, scene_id: str, file_name: str) -> dict[str, Any]
```

```text
Load scene state.

Args:
    scene_id (str): Stable identifier of the scene being processed.
    file_name (str): File name used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler.load_shot_state`

Defined at line 335.

```python
def load_shot_state(self, scene_id: str, file_name: str) -> dict[str, Any]
```

```text
Load shot state.

Args:
    scene_id (str): Stable identifier of the scene being processed.
    file_name (str): File name used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler.load_generation_tools`

Defined at line 368.

```python
def load_generation_tools(self) -> dict[str, dict[str, Any]]
```

```text
Load and validate configurable external generation tools.

Returns:
    dict[str, dict[str, Any]]: Result produced by the operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler.load_editing_models`

Defined at line 419.

```python
def load_editing_models(self) -> dict[str, Any]
```

```text
Load local editing-stack declarations without resolving artifacts.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler.load_wan_models`

Defined at line 483.

```python
def load_wan_models(self) -> dict[str, Any]
```

```text
Load the non-animation Wan 2.2 manifest without resolving weights.

Returns:
    dict[str, Any]: Validated configuration for T2V, I2V, TI2V, and S2V.

Raises:
    ContextCompilerError: If download protection, mode coverage, or animation
        exclusion is missing.
```

#### `ContextCompiler.load_workflow`

Defined at line 536.

```python
def load_workflow(self, workflow_id: str) -> dict[str, Any]
```

```text
Load one mode-specific ComfyUI API workflow binding.

Args:
    workflow_id (str): Workflow id used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler.load_production_plan`

Defined at line 597.

```python
def load_production_plan(self, scene_id: str) -> dict[str, Any]
```

```text
Load the tool and workflow selection for a scene.

Args:
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler.compile_production_scene`

Defined at line 640.

```python
def compile_production_scene(self, story_id: str, scene_id: str, require_enabled_tools: bool = False) -> dict[str, Any]
```

```text
Resolve scene context, production tools, and used workflows.

Args:
    story_id (str): Stable identifier of the story to load or compile.
    scene_id (str): Stable identifier of the scene being processed.
    require_enabled_tools (bool): Require enabled tools used by this operation.
        Defaults to ``False``.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler.load_validation_defaults`

Defined at line 701.

```python
def load_validation_defaults(self) -> dict[str, Any]
```

```text
Load validation rules shared by all scenes.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler.load_scene_validation`

Defined at line 733.

```python
def load_scene_validation(self, scene_id: str) -> dict[str, Any]
```

```text
Load scene and shot-specific validation expectations.

Args:
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler.compile_validation_scene`

Defined at line 771.

```python
def compile_validation_scene(self, story_id: str, scene_id: str) -> dict[str, Any]
```

```text
Compile production context together with effective validation rules.

Args:
    story_id (str): Stable identifier of the story to load or compile.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `ContextCompiler._validate_validation_shot_ids`

Defined at line 799.

```python
def _validate_validation_shot_ids(scene: dict[str, Any], validation: dict[str, Any]) -> None
```

```text
Validate validation shot ids.

Args:
    scene (dict[str, Any]): Scene used by this operation.
    validation (dict[str, Any]): Validation used by this operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler._deep_merge`

Defined at line 831.

```python
def _deep_merge(base: dict[str, Any], override: dict[str, Any]) -> dict[str, Any]
```

```text
Execute merge.

Args:
    base (dict[str, Any]): Base used by this operation.
    override (dict[str, Any]): Override used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `ContextCompiler._collect_tool_ids`

Defined at line 853.

```python
def _collect_tool_ids(production: dict[str, Any]) -> set[str]
```

```text
Collect tool ids.

Args:
    production (dict[str, Any]): Production used by this operation.

Returns:
    set[str]: Result produced by the operation.
```

#### `ContextCompiler._load_scene_shots`

Defined at line 885.

```python
def _load_scene_shots(self, scene: dict[str, Any]) -> list[dict[str, Any]]
```

```text
Load scene shots.

Args:
    scene (dict[str, Any]): Scene used by this operation.

Returns:
    list[dict[str, Any]]: Result produced by the operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler._load_scene_props`

Defined at line 907.

```python
def _load_scene_props(self, scene: dict[str, Any]) -> dict[str, dict[str, Any]]
```

```text
Load scene props.

Args:
    scene (dict[str, Any]): Scene used by this operation.

Returns:
    dict[str, dict[str, Any]]: Result produced by the operation.
```

#### `ContextCompiler._load_scene_wardrobe`

Defined at line 923.

```python
def _load_scene_wardrobe(self, scene: dict[str, Any]) -> dict[str, dict[str, Any]]
```

```text
Load scene wardrobe.

Args:
    scene (dict[str, Any]): Scene used by this operation.

Returns:
    dict[str, dict[str, Any]]: Result produced by the operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler._load_scene_continuity`

Defined at line 955.

```python
def _load_scene_continuity(self, scene: dict[str, Any]) -> dict[str, Any]
```

```text
Load scene continuity.

Args:
    scene (dict[str, Any]): Scene used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `ContextCompiler._apply_shot_defaults`

Defined at line 974.

```python
def _apply_shot_defaults(self, shot: dict[str, Any]) -> dict[str, Any]
```

```text
Apply shot defaults.

Args:
    shot (dict[str, Any]): Shot used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `ContextCompiler._validate_shot`

Defined at line 998.

```python
def _validate_shot(self, shot: dict[str, Any], path: Path) -> None
```

```text
Validate shot.

Args:
    shot (dict[str, Any]): Shot used by this operation.
    path (Path): Filesystem path read or written by the operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler._validate_scene_references`

Defined at line 1044.

```python
def _validate_scene_references(self, scene: dict[str, Any], shots: list[dict[str, Any]], props: dict[str, dict[str, Any]], wardrobe: dict[str, dict[str, Any]], continuity: dict[str, Any]) -> None
```

```text
Validate scene references.

Args:
    scene (dict[str, Any]): Scene used by this operation.
    shots (list[dict[str, Any]]): Shots used by this operation.
    props (dict[str, dict[str, Any]]): Props used by this operation.
    wardrobe (dict[str, dict[str, Any]]): Wardrobe used by this operation.
    continuity (dict[str, Any]): Continuity state approved for subsequent
        production.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler._validate_state_block`

Defined at line 1144.

```python
def _validate_state_block(state: dict[str, Any], state_name: str, known_character_ids: set[str], known_prop_ids: set[str], wardrobe: dict[str, dict[str, Any]]) -> None
```

```text
Validate state block.

Args:
    state (dict[str, Any]): State used by this operation.
    state_name (str): State name used by this operation.
    known_character_ids (set[str]): Known character ids used by this operation.
    known_prop_ids (set[str]): Known prop ids used by this operation.
    wardrobe (dict[str, dict[str, Any]]): Wardrobe used by this operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler._validate_generation_inputs`

Defined at line 1206.

```python
def _validate_generation_inputs(shot: dict[str, Any], generation: dict[str, Any]) -> None
```

```text
Validate generation inputs.

Args:
    shot (dict[str, Any]): Shot used by this operation.
    generation (dict[str, Any]): Generation used by this operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler._validate_dialogue`

Defined at line 1230.

```python
def _validate_dialogue(shot: dict[str, Any], generation_mode: str) -> None
```

```text
Validate dialogue.

Args:
    shot (dict[str, Any]): Shot used by this operation.
    generation_mode (str): Generation mode used by this operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler._validate_story_scene_relationship`

Defined at line 1268.

```python
def _validate_story_scene_relationship(story: dict[str, Any], scene: dict[str, Any]) -> None
```

```text
Validate story scene relationship.

Args:
    story (dict[str, Any]): Story used by this operation.
    scene (dict[str, Any]): Scene used by this operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler._validate_unique_shot_ids`

Defined at line 1290.

```python
def _validate_unique_shot_ids(shots: list[dict[str, Any]]) -> None
```

```text
Validate unique shot ids.

Args:
    shots (list[dict[str, Any]]): Shots used by this operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler._load_yaml`

Defined at line 1308.

```python
def _load_yaml(self, path: Path) -> dict[str, Any]
```

```text
Load yaml.

Args:
    path (Path): Filesystem path read or written by the operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler._extract_root_object`

Defined at line 1343.

```python
def _extract_root_object(document: dict[str, Any], root_key: str, path: Path) -> dict[str, Any]
```

```text
Extract root object.

Args:
    document (dict[str, Any]): Structured document to validate, merge, or
        persist.
    root_key (str): Root key used by this operation.
    path (Path): Filesystem path read or written by the operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler._require_string`

Defined at line 1368.

```python
def _require_string(values: dict[str, Any], field_name: str, path: Path) -> None
```

```text
Execute string.

Args:
    values (dict[str, Any]): Values used by this operation.
    field_name (str): Field name used by this operation.
    path (Path): Filesystem path read or written by the operation.

Raises:
    ContextCompilerError: If inputs, context, state, or provider output are invalid.
```

#### `ContextCompiler._merge_defaults`

Defined at line 1388.

```python
def _merge_defaults(defaults: dict[str, Any], values: Any) -> dict[str, Any]
```

```text
Execute defaults.

Args:
    defaults (dict[str, Any]): Defaults used by this operation.
    values (Any): Values used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

## `lpw.context.defaults`

Source: [`src/lpw/context/defaults.py`](../src/lpw/context/defaults.py#L1)

```text
Define immutable defaults and supported values for this package.
```

## `lpw.context.loader`

Source: [`src/lpw/context/loader.py`](../src/lpw/context/loader.py#L1)

```text
Provide loader services for the LPW cinematic pipeline.
```

### `get_project_directory`

Defined at line 16.

```python
def get_project_directory(project_id: str) -> Path
```

```text
Return project directory.

Args:
    project_id (str): Stable identifier of the project whose context is used.

Returns:
    Path: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
    ContextNotFoundError: If inputs, context, state, or provider output are invalid.
```

### `_load_first`

Defined at line 39.

```python
def _load_first(paths: Iterable[Path], *, required: bool = False) -> dict[str, Any]
```

```text
Load first.

Args:
    paths (Iterable[Path]): Paths used by this operation.
    required (bool): Whether absence of the requested file is an error. Defaults to
        ``False``.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ContextNotFoundError: If inputs, context, state, or provider output are invalid.
```

### `_load_directory`

Defined at line 63.

```python
def _load_directory(directory: Path) -> dict[str, Any]
```

```text
Load directory.

Args:
    directory (Path): Directory used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `load_character`

Defined at line 79.

```python
def load_character(project_directory: Path, character_id: str) -> dict[str, Any]
```

```text
Load character.

Args:
    project_directory (Path): Project directory used by this operation.
    character_id (str): Stable identifier of the character.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `load_location`

Defined at line 99.

```python
def load_location(project_directory: Path, location_id: str) -> dict[str, Any]
```

```text
Load location.

Args:
    project_directory (Path): Project directory used by this operation.
    location_id (str): Stable identifier of the location.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `audio_directory`

Defined at line 119.

```python
def audio_directory(project_directory: Path) -> Path
```

```text
Execute directory.

Args:
    project_directory (Path): Project directory used by this operation.

Returns:
    Path: Result produced by the operation.
```

### `load_audio_context`

Defined at line 135.

```python
def load_audio_context(project_id: str) -> dict[str, Any]
```

```text
Load audio context.

Args:
    project_id (str): Stable identifier of the project whose context is used.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `load_voice`

Defined at line 175.

```python
def load_voice(project_id: str, character_id: str) -> dict[str, Any]
```

```text
Load voice.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    character_id (str): Stable identifier of the character.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `load_project_context`

Defined at line 190.

```python
def load_project_context(project_id: str, *, character_ids: list[str] | None = None, location_id: str | None = None) -> dict[str, Any]
```

```text
Load the project's flat or nested schema without modifying source files.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    character_ids (list[str] | None): Character identifiers whose context should be
        included. Defaults to ``None``.
    location_id (str | None): Stable identifier of the location. Defaults to
        ``None``.

Returns:
    dict[str, Any]: Result produced by the operation.
```

## `lpw.context.pipeline`

Source: [`src/lpw/context/pipeline.py`](../src/lpw/context/pipeline.py#L1)

```text
Provide pipeline services for the LPW cinematic pipeline.
```

### `SceneContextPipeline`

Defined at line 12.

```python
class SceneContextPipeline
```

```text
Provide validated scene context to generation and MCP services.
```

#### `SceneContextPipeline.__init__`

Defined at line 15.

```python
def __init__(self, context_root: Path | str = DEFAULT_CONTEXT_ROOT) -> None
```

```text
Initialize the service with its configured dependencies.

Args:
    context_root (Path | str): Root directory containing source context files.
        Defaults to ``DEFAULT_CONTEXT_ROOT``.
```

#### `SceneContextPipeline.load_scene`

Defined at line 24.

```python
def load_scene(self, story_id: str, scene_id: str) -> dict[str, Any]
```

```text
Load scene.

Args:
    story_id (str): Stable identifier of the story to load or compile.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `SceneContextPipeline.get_scene_summary`

Defined at line 36.

```python
def get_scene_summary(self, story_id: str, scene_id: str) -> dict[str, Any]
```

```text
Return scene summary.

Args:
    story_id (str): Stable identifier of the story to load or compile.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `SceneContextPipeline.iter_shots`

Defined at line 65.

```python
def iter_shots(self, story_id: str, scene_id: str) -> Iterator[dict[str, Any]]
```

```text
Iterate over shots.

Args:
    story_id (str): Stable identifier of the story to load or compile.
    scene_id (str): Stable identifier of the scene being processed.

Yields:
    Iterator[dict[str, Any]]: Result produced by the operation.
```

#### `SceneContextPipeline.build_generation_plan`

Defined at line 85.

```python
def build_generation_plan(self, story_id: str, scene_id: str) -> list[dict[str, Any]]
```

```text
Build generation plan.

Args:
    story_id (str): Stable identifier of the story to load or compile.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    list[dict[str, Any]]: Result produced by the operation.
```

#### `SceneContextPipeline.get_scene_opening_state`

Defined at line 146.

```python
def get_scene_opening_state(self, story_id: str, scene_id: str) -> dict[str, Any]
```

```text
Return scene opening state.

Args:
    story_id (str): Stable identifier of the story to load or compile.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `SceneContextPipeline.get_scene_expected_end_state`

Defined at line 161.

```python
def get_scene_expected_end_state(self, story_id: str, scene_id: str) -> dict[str, Any]
```

```text
Return scene expected end state.

Args:
    story_id (str): Stable identifier of the story to load or compile.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    dict[str, Any]: Result produced by the operation.
```

## `lpw.editing`

Source: [`src/lpw/editing/__init__.py`](../src/lpw/editing/__init__.py#L1)

```text
Expose the public editing package API for LPW.
```

## `lpw.editing.automation`

Source: [`src/lpw/editing/automation.py`](../src/lpw/editing/automation.py#L1)

```text
Provide automation services for the LPW cinematic pipeline.
```

### `AutomatedEditingPipeline`

Defined at line 31.

```python
class AutomatedEditingPipeline
```

```text
Controlled preparation, assisted editing, and post-edit orchestration.
```

#### `AutomatedEditingPipeline.__init__`

Defined at line 34.

```python
def __init__(self, project_root: Path | str = PROJECT_ROOT) -> None
```

```text
Initialize the service with its configured dependencies.

Args:
    project_root (Path | str): Root directory containing project runtime data.
        Defaults to ``PROJECT_ROOT``.
```

#### `AutomatedEditingPipeline.prepare_automated_edit`

Defined at line 44.

```python
def prepare_automated_edit(self, project_id: str, scene_id: str) -> dict[str, Any]
```

```text
Prepare automated edit.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `AutomatedEditingPipeline.automate_scene_edit`

Defined at line 207.

```python
def automate_scene_edit(self, project_id: str, scene_id: str, prepared_package_version: int) -> dict[str, Any]
```

```text
Execute scene edit.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    prepared_package_version (int): Version of the prepared editing package.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `AutomatedEditingPipeline.approve_edit_preview`

Defined at line 316.

```python
def approve_edit_preview(self, project_id: str, scene_id: str, edit_version: int, reviewer: str, notes: list[dict[str, Any]] | None = None) -> dict[str, Any]
```

```text
Approve edit preview.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    edit_version (int): Positive version number of the edit.
    reviewer (str): Human reviewer identity recorded with the decision.
    notes (list[dict[str, Any]] | None): Optional review or production notes.
        Defaults to ``None``.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `AutomatedEditingPipeline.post_edit_scene`

Defined at line 365.

```python
def post_edit_scene(self, project_id: str, scene_id: str, approved_edit_version: int, review_notes: list[dict[str, Any]]) -> dict[str, Any]
```

```text
Execute edit scene.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    approved_edit_version (int): Version of the edit approved for post-
        production.
    review_notes (list[dict[str, Any]]): Structured or textual notes supplied by
        the reviewer.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `AutomatedEditingPipeline.load_prepared_package`

Defined at line 445.

```python
def load_prepared_package(self, project_id: str, scene_id: str, version: int) -> dict[str, Any]
```

```text
Load prepared package.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    version (int): Positive version number of the stored artifact.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

#### `AutomatedEditingPipeline.load_latest_prepared_package`

Defined at line 472.

```python
def load_latest_prepared_package(self, project_id: str, scene_id: str) -> dict[str, Any]
```

```text
Load latest prepared package.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    FileNotFoundError: If inputs, context, state, or provider output are invalid.
```

#### `AutomatedEditingPipeline._render_preview`

Defined at line 495.

```python
def _render_preview(self, plan: dict[str, Any], preview_rules: dict[str, Any], directory: Path) -> Path
```

```text
Render preview.

Args:
    plan (dict[str, Any]): Plan used by this operation.
    preview_rules (dict[str, Any]): Preview rules used by this operation.
    directory (Path): Directory used by this operation.

Returns:
    Path: Result produced by the operation.
```

#### `AutomatedEditingPipeline._select_best_takes`

Defined at line 532.

```python
def _select_best_takes(takes: list[dict[str, Any]]) -> list[dict[str, Any]]
```

```text
Select best takes.

Args:
    takes (list[dict[str, Any]]): Takes used by this operation.

Returns:
    list[dict[str, Any]]: Result produced by the operation.
```

#### `AutomatedEditingPipeline._weighted_score`

Defined at line 549.

```python
def _weighted_score(scores: Any, weights: Any) -> float
```

```text
Execute score.

Args:
    scores (Any): Named review scores used by approval thresholds.
    weights (Any): Weights used by this operation.

Returns:
    float: Result produced by the operation.
```

#### `AutomatedEditingPipeline._adjacency_warnings`

Defined at line 576.

```python
def _adjacency_warnings(previous: dict[str, Any], current: dict[str, Any]) -> list[dict[str, Any]]
```

```text
Execute warnings.

Args:
    previous (dict[str, Any]): Previous used by this operation.
    current (dict[str, Any]): Current used by this operation.

Returns:
    list[dict[str, Any]]: Result produced by the operation.
```

#### `AutomatedEditingPipeline._audio_events`

Defined at line 605.

```python
def _audio_events(assets: dict[str, str], duration: float) -> list[dict[str, Any]]
```

```text
Execute events.

Args:
    assets (dict[str, str]): Assets used by this operation.
    duration (float): Duration used by this operation.

Returns:
    list[dict[str, Any]]: Result produced by the operation.
```

#### `AutomatedEditingPipeline._note_to_operation`

Defined at line 635.

```python
def _note_to_operation(note: dict[str, Any]) -> dict[str, Any]
```

```text
Execute to operation.

Args:
    note (dict[str, Any]): Note used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `AutomatedEditingPipeline._validate_review_notes`

Defined at line 653.

```python
def _validate_review_notes(notes: list[dict[str, Any]]) -> list[dict[str, Any]]
```

```text
Validate review notes.

Args:
    notes (list[dict[str, Any]]): Optional review or production notes.

Returns:
    list[dict[str, Any]]: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `AutomatedEditingPipeline._load_timeline`

Defined at line 696.

```python
def _load_timeline(self, project_directory: Path, project_id: str, scene_id: str) -> dict[str, Any]
```

```text
Load timeline.

Args:
    project_directory (Path): Project directory used by this operation.
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `AutomatedEditingPipeline._resolve_project_path`

Defined at line 720.

```python
def _resolve_project_path(self, value: str) -> Path
```

```text
Resolve project path.

Args:
    value (str): Value inspected or transformed by the helper.

Returns:
    Path: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `AutomatedEditingPipeline._edit_directory`

Defined at line 744.

```python
def _edit_directory(self, project_id: str, scene_id: str, version: int) -> Path
```

```text
Execute directory.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    version (int): Positive version number of the stored artifact.

Returns:
    Path: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
    FileNotFoundError: If inputs, context, state, or provider output are invalid.
```

#### `AutomatedEditingPipeline._write_archive_manifest`

Defined at line 766.

```python
def _write_archive_manifest(self, project_id: str, scene_id: str, edit_version: int, edit_directory: Path, result: dict[str, Any]) -> dict[str, Any]
```

```text
Execute archive manifest.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    edit_version (int): Positive version number of the edit.
    edit_directory (Path): Edit directory used by this operation.
    result (dict[str, Any]): Result used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `AutomatedEditingPipeline._hash_file`

Defined at line 838.

```python
def _hash_file(path: Path) -> str
```

```text
Execute file.

Args:
    path (Path): Filesystem path read or written by the operation.

Returns:
    str: Result produced by the operation.
```

#### `AutomatedEditingPipeline._next_version`

Defined at line 854.

```python
def _next_version(directory: Path, prefix: str) -> int
```

```text
Execute version.

Args:
    directory (Path): Directory used by this operation.
    prefix (str): Prefix used by this operation.

Returns:
    int: Result produced by the operation.
```

#### `AutomatedEditingPipeline._identifiers`

Defined at line 876.

```python
def _identifiers(project_id: str, scene_id: str) -> tuple[str, str]
```

```text
Execute identifiers.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    tuple[str, str]: Result produced by the operation.
```

#### `AutomatedEditingPipeline._now_iso`

Defined at line 892.

```python
def _now_iso() -> str
```

```text
Execute iso.

Returns:
    str: Result produced by the operation.
```

## `lpw.editing.planner`

Source: [`src/lpw/editing/planner.py`](../src/lpw/editing/planner.py#L1)

```text
Provide planner services for the LPW cinematic pipeline.
```

### `load_shot_metadata`

Defined at line 13.

```python
def load_shot_metadata(metadata_path: str) -> dict[str, Any]
```

```text
Load shot metadata.

Args:
    metadata_path (str): Metadata path used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `create_edit_plan`

Defined at line 25.

```python
def create_edit_plan(project_id: str, shot_metadata_files: list[str], reference_shot_id: str, *, require_existing_sources: bool = False) -> dict[str, Any]
```

```text
Create edit plan.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    shot_metadata_files (list[str]): Shot metadata files used by this operation.
    reference_shot_id (str): Reference shot id used by this operation.
    require_existing_sources (bool): Require existing sources used by this
        operation. Defaults to ``False``.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

### `edit_cinematic_sequence`

Defined at line 102.

```python
def edit_cinematic_sequence(project_id: str, scene_id: str, shot_metadata_files: list[str], reference_shot_id: str, output_path: str) -> dict[str, Any]
```

```text
Execute cinematic sequence.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_metadata_files (list[str]): Shot metadata files used by this operation.
    reference_shot_id (str): Reference shot id used by this operation.
    output_path (str): Destination path for the generated output.

Returns:
    dict[str, Any]: Result produced by the operation.
```

## `lpw.editing.state`

Source: [`src/lpw/editing/state.py`](../src/lpw/editing/state.py#L1)

```text
Provide state services for the LPW cinematic pipeline.
```

### `build_continuity_state`

Defined at line 13.

```python
def build_continuity_state(project_id: str, scene_id: str, edit_version: int, last_shot: dict[str, Any]) -> dict[str, Any]
```

```text
Build continuity state.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    edit_version (int): Positive version number of the edit.
    last_shot (dict[str, Any]): Last shot used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

### `save_continuity_state`

Defined at line 54.

```python
def save_continuity_state(project_id: str, scene_id: str, edit_version: int, last_shot: dict[str, Any]) -> str
```

```text
Save generated edit state under /runtime, never under read-only /context.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    edit_version (int): Positive version number of the edit.
    last_shot (dict[str, Any]): Last shot used by this operation.

Returns:
    str: Result produced by the operation.
```

### `commit_scene_continuity`

Defined at line 76.

```python
def commit_scene_continuity(*, project_id: str, scene_id: str, edit_version: int, last_shot_approval: dict[str, Any], output_path: Path) -> dict[str, Any]
```

```text
Commit authoritative continuity only after a successful final export.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    edit_version (int): Positive version number of the edit.
    last_shot_approval (dict[str, Any]): Last shot approval used by this operation.
    output_path (Path): Destination path for the generated output.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

## `lpw.editing.timeline`

Source: [`src/lpw/editing/timeline.py`](../src/lpw/editing/timeline.py#L1)

```text
Provide timeline services for the LPW cinematic pipeline.
```

### `run_command`

Defined at line 25.

```python
def run_command(arguments: list[str]) -> None
```

```text
Run command.

Args:
    arguments (list[str]): Command arguments passed without shell interpolation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

### `normalize_clip`

Defined at line 53.

```python
def normalize_clip(*, source: Path, destination: Path, in_seconds: float, out_seconds: float, video_rules: dict[str, Any]) -> Path
```

```text
Normalize clip.

Args:
    source (Path): Source value or filesystem path consumed by the operation.
    destination (Path): Destination path for the transformed asset.
    in_seconds (float): In seconds used by this operation.
    out_seconds (float): Out seconds used by this operation.
    video_rules (dict[str, Any]): Video rules used by this operation.

Returns:
    Path: Result produced by the operation.
```

### `concatenate_clips`

Defined at line 109.

```python
def concatenate_clips(clip_paths: list[Path], output_path: Path) -> Path
```

```text
Concatenate clips.

Args:
    clip_paths (list[Path]): Clip paths used by this operation.
    output_path (Path): Destination path for the generated output.

Returns:
    Path: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

### `mix_scene_audio`

Defined at line 149.

```python
def mix_scene_audio(*, silent_video: Path, stems: dict[str, Path], output_path: Path, video_rules: dict[str, Any], audio_rules: dict[str, Any]) -> Path
```

```text
Mix scene audio.

Args:
    silent_video (Path): Silent video used by this operation.
    stems (dict[str, Path]): Stems used by this operation.
    output_path (Path): Destination path for the generated output.
    video_rules (dict[str, Any]): Video rules used by this operation.
    audio_rules (dict[str, Any]): Audio rules used by this operation.

Returns:
    Path: Result produced by the operation.
```

### `SceneFinalizationService`

Defined at line 228.

```python
class SceneFinalizationService
```

```text
Validate, assemble, release, then commit continuity for one scene.
```

#### `SceneFinalizationService.__init__`

Defined at line 231.

```python
def __init__(self, project_root: Path | str = PROJECT_ROOT) -> None
```

```text
Initialize the service with its configured dependencies.

Args:
    project_root (Path | str): Root directory containing project runtime data.
        Defaults to ``PROJECT_ROOT``.
```

#### `SceneFinalizationService.finalize_scene`

Defined at line 243.

```python
def finalize_scene(self, project_id: str, scene_id: str, edit_version: int = 1) -> dict[str, Any]
```

```text
Finalize scene.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    edit_version (int): Positive version number of the edit. Defaults to ``1``.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `SceneFinalizationService._validate_context`

Defined at line 464.

```python
def _validate_context(project_id: str, scene_id: str, timeline: dict[str, Any], export: dict[str, Any]) -> None
```

```text
Validate context.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    timeline (dict[str, Any]): Timeline used by this operation.
    export (dict[str, Any]): Export used by this operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `SceneFinalizationService._resolve_project_file`

Defined at line 530.

```python
def _resolve_project_file(self, value: str) -> Path
```

```text
Resolve project file.

Args:
    value (str): Value inspected or transformed by the helper.

Returns:
    Path: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

## `lpw.editing.validation`

Source: [`src/lpw/editing/validation.py`](../src/lpw/editing/validation.py#L1)

```text
Provide validation services for the LPW cinematic pipeline.
```

### `_continuity_warnings`

Defined at line 14.

```python
def _continuity_warnings(previous: dict[str, Any], current: dict[str, Any]) -> list[str]
```

```text
Execute warnings.

Args:
    previous (dict[str, Any]): Previous used by this operation.
    current (dict[str, Any]): Current used by this operation.

Returns:
    list[str]: Result produced by the operation.
```

### `validate_edit_plan`

Defined at line 54.

```python
def validate_edit_plan(edit_plan: dict[str, Any]) -> dict[str, list[str]]
```

```text
Validate visual, continuity, editing, and audio requirements.

Args:
    edit_plan (dict[str, Any]): Edit plan used by this operation.

Returns:
    dict[str, list[str]]: Result produced by the operation.
```

## `lpw.errors`

Source: [`src/lpw/errors.py`](../src/lpw/errors.py#L1)

```text
Define domain-specific exceptions raised by LPW services.
```

### `ContextError`

Defined at line 3.

```python
class ContextError(Exception)
```

```text
Base error for cinematic context problems.
```

### `ContextNotFoundError`

Defined at line 7.

```python
class ContextNotFoundError(ContextError)
```

```text
Raised when required context is unavailable.
```

### `InvalidContextIdentifierError`

Defined at line 11.

```python
class InvalidContextIdentifierError(ContextError)
```

```text
Raised when an identifier could escape its expected directory.
```

### `InvalidContextDataError`

Defined at line 15.

```python
class InvalidContextDataError(ContextError)
```

```text
Raised when a context document has an invalid root value.
```

## `lpw.generation`

Source: [`src/lpw/generation/__init__.py`](../src/lpw/generation/__init__.py#L1)

```text
Expose the public generation package API for LPW.
```

## `lpw.generation.compiler`

Source: [`src/lpw/generation/compiler.py`](../src/lpw/generation/compiler.py#L1)

```text
Provide compiler services for the LPW cinematic pipeline.
```

### `select_wan_model`

Defined at line 19.

```python
def select_wan_model(request: ShotRequest) -> tuple[str, str]
```

```text
Select wan model.

Args:
    request (ShotRequest): Typed request containing the inputs for this operation.

Returns:
    tuple[str, str]: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

### `calculate_frame_count`

Defined at line 50.

```python
def calculate_frame_count(duration_seconds: int, frame_rate: int = 24) -> int
```

```text
Calculate frame count.

Args:
    duration_seconds (int): Requested duration in seconds.
    frame_rate (int): Frames per second used for timing calculations. Defaults to
        ``24``.

Returns:
    int: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

### `select_primary_reference`

Defined at line 72.

```python
def select_primary_reference(request: ShotRequest, context: dict[str, Any]) -> tuple[str | None, list[str]]
```

```text
Select primary reference.

Args:
    request (ShotRequest): Typed request containing the inputs for this operation.
    context (dict[str, Any]): Resolved cinematic context used by the operation.

Returns:
    tuple[str | None, list[str]]: Result produced by the operation.
```

### `generate_stable_seed`

Defined at line 103.

```python
def generate_stable_seed(request: ShotRequest, context_hash: str) -> int
```

```text
Generate stable seed.

Args:
    request (ShotRequest): Typed request containing the inputs for this operation.
    context_hash (str): Context hash used by this operation.

Returns:
    int: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

### `compile_wan_shot_package`

Defined at line 124.

```python
def compile_wan_shot_package(request: ShotRequest) -> WanShotPackage
```

```text
Compile wan shot package.

Args:
    request (ShotRequest): Typed request containing the inputs for this operation.

Returns:
    WanShotPackage: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

## `lpw.generation.extension`

Source: [`src/lpw/generation/extension.py`](../src/lpw/generation/extension.py#L1)

```text
Provide extension services for the LPW cinematic pipeline.
```

### `CinematicExtensionPipeline`

Defined at line 20.

```python
class CinematicExtensionPipeline
```

```text
Plan and audit short chained segments without invoking model downloads.
```

#### `CinematicExtensionPipeline.__init__`

Defined at line 23.

```python
def __init__(self, source_root: Path | str | None = None, state_root: Path | str | None = None) -> None
```

```text
Initialize the service with its configured dependencies.

Args:
    source_root (Path | str | None): Root directory containing immutable source
        context. Defaults to ``None``.
    state_root (Path | str | None): Root directory used for generated runtime
        state. Defaults to ``None``.
```

#### `CinematicExtensionPipeline.resolve_context_chain`

Defined at line 40.

```python
def resolve_context_chain(self, project_id: str, scene_id: str, shot_id: str) -> dict[str, Any]
```

```text
Resolve context chain.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `CinematicExtensionPipeline.validate_context_chain`

Defined at line 61.

```python
def validate_context_chain(self, project_id: str, scene_id: str, shot_id: str) -> dict[str, Any]
```

```text
Validate context chain.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `CinematicExtensionPipeline.extend_cinematic_shot`

Defined at line 86.

```python
def extend_cinematic_shot(self, project_id: str, scene_id: str, shot_id: str, target_duration_seconds: float, segment_duration_seconds: float = 4.0) -> dict[str, Any]
```

```text
Execute cinematic shot.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    target_duration_seconds (float): Total desired duration of the extended
        shot.
    segment_duration_seconds (float): Maximum duration of each generated
        segment. Defaults to ``4.0``.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

#### `CinematicExtensionPipeline.compile_segment_context`

Defined at line 186.

```python
def compile_segment_context(self, project_id: str, scene_id: str, shot_id: str, extension_version: int, segment_id: str) -> dict[str, Any]
```

```text
Compile segment context.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    extension_version (int): Positive version number of the shot extension.
    segment_id (str): Stable identifier of the chained video segment.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `CinematicExtensionPipeline.approve_segment`

Defined at line 263.

```python
def approve_segment(self, project_id: str, scene_id: str, shot_id: str, extension_version: int, segment_id: str, generated_video_path: str, approved_end_frame_path: str, generated_frames: int, approved_end_frame: int, continuity_delta: dict[str, Any], analysis: dict[str, Any], reviewer: str) -> dict[str, Any]
```

```text
Approve segment.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    extension_version (int): Positive version number of the shot extension.
    segment_id (str): Stable identifier of the chained video segment.
    generated_video_path (str): Path to the real video returned by a provider.
    approved_end_frame_path (str): Path to the reviewed stable segment end
        frame.
    generated_frames (int): Total number of frames produced by the provider.
    approved_end_frame (int): Index of the reviewed stable final frame.
    continuity_delta (dict[str, Any]): Small state delta produced by an approved
        segment.
    analysis (dict[str, Any]): Structured provider or validation analysis
        results.
    reviewer (str): Human reviewer identity recorded with the decision.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
    FileNotFoundError: If inputs, context, state, or provider output are invalid.
```

#### `CinematicExtensionPipeline.runtime_end_state`

Defined at line 397.

```python
def runtime_end_state(self, segment_id: str) -> dict[str, Any]
```

```text
Execute end state.

Args:
    segment_id (str): Stable identifier of the chained video segment.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `CinematicExtensionPipeline._compile_manifest`

Defined at line 414.

```python
def _compile_manifest(self, *, base: dict[str, Any], manifest: dict[str, Any], directory: Path, runtime_state: dict[str, Any]) -> dict[str, Any]
```

```text
Compile manifest.

Args:
    base (dict[str, Any]): Base used by this operation.
    manifest (dict[str, Any]): Manifest used by this operation.
    directory (Path): Directory used by this operation.
    runtime_state (dict[str, Any]): Authoritative runtime state applied to the
        next operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `CinematicExtensionPipeline._segment_manifest`

Defined at line 451.

```python
def _segment_manifest(self, *, project_id: str, scene_id: str, shot_id: str, segment_id: str, index: int, count: int, start: float, end: float) -> dict[str, Any]
```

```text
Execute manifest.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    segment_id (str): Stable identifier of the chained video segment.
    index (int): Index used by this operation.
    count (int): Count used by this operation.
    start (float): Start used by this operation.
    end (float): End used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `CinematicExtensionPipeline._mark_approved`

Defined at line 500.

```python
def _mark_approved(self, directory: Path, segment_id: str) -> None
```

```text
Execute approved.

Args:
    directory (Path): Directory used by this operation.
    segment_id (str): Stable identifier of the chained video segment.
```

#### `CinematicExtensionPipeline._previous_segment`

Defined at line 525.

```python
def _previous_segment(plan: dict[str, Any], segment_id: str) -> str | None
```

```text
Execute segment.

Args:
    plan (dict[str, Any]): Plan used by this operation.
    segment_id (str): Stable identifier of the chained video segment.

Returns:
    str | None: Result produced by the operation.
```

#### `CinematicExtensionPipeline._shot_uri`

Defined at line 539.

```python
def _shot_uri(self, project_id: str, scene_id: str, shot_id: str) -> str
```

```text
Execute uri.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.

Returns:
    str: Result produced by the operation.

Raises:
    ContextChainError: If inputs, context, state, or provider output are invalid.
```

#### `CinematicExtensionPipeline._next_version`

Defined at line 574.

```python
def _next_version(self, project_id: str, scene_id: str, shot_id: str) -> int
```

```text
Execute version.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.

Returns:
    int: Result produced by the operation.
```

#### `CinematicExtensionPipeline._extension_directory`

Defined at line 593.

```python
def _extension_directory(self, project_id: str, scene_id: str, shot_id: str, version: int) -> Path
```

```text
Execute directory.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    version (int): Positive version number of the stored artifact.

Returns:
    Path: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

#### `CinematicExtensionPipeline._identifiers`

Defined at line 622.

```python
def _identifiers(project_id: str, scene_id: str, shot_id: str) -> tuple[str, str, str]
```

```text
Execute identifiers.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.

Returns:
    tuple[str, str, str]: Result produced by the operation.
```

#### `CinematicExtensionPipeline._now_iso`

Defined at line 642.

```python
def _now_iso() -> str
```

```text
Execute iso.

Returns:
    str: Result produced by the operation.
```

## `lpw.generation.pipeline`

Source: [`src/lpw/generation/pipeline.py`](../src/lpw/generation/pipeline.py#L1)

```text
Provide pipeline services for the LPW cinematic pipeline.
```

### `GenerationPipelineError`

Defined at line 23.

```python
class GenerationPipelineError(Exception)
```

```text
Raised when a configured generation or editing stage fails.
```

### `SceneGenerationPipeline`

Defined at line 27.

```python
class SceneGenerationPipeline
```

```text
Produce a complete scene from compiled YAML context and configured tools.
```

#### `SceneGenerationPipeline.__init__`

Defined at line 30.

```python
def __init__(self, project_root: Path | str, context_root: Path | str = DEFAULT_CONTEXT_ROOT) -> None
```

```text
Initialize the service with its configured dependencies.

Args:
    project_root (Path | str): Root directory containing project runtime data.
    context_root (Path | str): Root directory containing source context files.
        Defaults to ``DEFAULT_CONTEXT_ROOT``.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `SceneGenerationPipeline.produce_scene`

Defined at line 53.

```python
def produce_scene(self, story_id: str, scene_id: str, dry_run: bool = True) -> dict[str, Any]
```

```text
Execute scene.

Args:
    story_id (str): Stable identifier of the story to load or compile.
    scene_id (str): Stable identifier of the scene being processed.
    dry_run (bool): Whether to plan without calling external providers. Defaults
        to ``True``.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `SceneGenerationPipeline._produce_shot`

Defined at line 123.

```python
def _produce_shot(self, package: dict[str, Any], shot: dict[str, Any], scene_output_directory: Path) -> dict[str, Any]
```

```text
Execute shot.

Args:
    package (dict[str, Any]): Compiled package consumed by the operation.
    shot (dict[str, Any]): Shot used by this operation.
    scene_output_directory (Path): Scene output directory used by this
        operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `SceneGenerationPipeline._resolve_dialogue_audio`

Defined at line 181.

```python
def _resolve_dialogue_audio(self, package: dict[str, Any], shot: dict[str, Any], shot_directory: Path) -> Path | None
```

```text
Resolve dialogue audio.

Args:
    package (dict[str, Any]): Compiled package consumed by the operation.
    shot (dict[str, Any]): Shot used by this operation.
    shot_directory (Path): Shot directory used by this operation.

Returns:
    Path | None: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `SceneGenerationPipeline._resolve_reference_image`

Defined at line 228.

```python
def _resolve_reference_image(self, package: dict[str, Any], shot: dict[str, Any], shot_directory: Path) -> Path | None
```

```text
Resolve reference image.

Args:
    package (dict[str, Any]): Compiled package consumed by the operation.
    shot (dict[str, Any]): Shot used by this operation.
    shot_directory (Path): Shot directory used by this operation.

Returns:
    Path | None: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `SceneGenerationPipeline._generate_wan_clip`

Defined at line 278.

```python
def _generate_wan_clip(self, package: dict[str, Any], shot: dict[str, Any], shot_directory: Path) -> Path
```

```text
Generate wan clip.

Args:
    package (dict[str, Any]): Compiled package consumed by the operation.
    shot (dict[str, Any]): Shot used by this operation.
    shot_directory (Path): Shot directory used by this operation.

Returns:
    Path: Result produced by the operation.
```

#### `SceneGenerationPipeline._apply_optional_lip_sync`

Defined at line 320.

```python
def _apply_optional_lip_sync(self, package: dict[str, Any], shot: dict[str, Any], generated_clip: Path, dialogue_audio: Path | None, shot_directory: Path) -> Path
```

```text
Apply optional lip sync.

Args:
    package (dict[str, Any]): Compiled package consumed by the operation.
    shot (dict[str, Any]): Shot used by this operation.
    generated_clip (Path): Generated clip used by this operation.
    dialogue_audio (Path | None): Dialogue audio used by this operation.
    shot_directory (Path): Shot directory used by this operation.

Returns:
    Path: Result produced by the operation.
```

#### `SceneGenerationPipeline._normalize_shot_segment`

Defined at line 356.

```python
def _normalize_shot_segment(self, package: dict[str, Any], shot: dict[str, Any], video_file: Path, dialogue_audio: Path | None, shot_directory: Path) -> Path
```

```text
Normalize shot segment.

Args:
    package (dict[str, Any]): Compiled package consumed by the operation.
    shot (dict[str, Any]): Shot used by this operation.
    video_file (Path): Path to the video file associated with the operation.
    dialogue_audio (Path | None): Dialogue audio used by this operation.
    shot_directory (Path): Shot directory used by this operation.

Returns:
    Path: Result produced by the operation.
```

#### `SceneGenerationPipeline._concatenate_segments`

Defined at line 424.

```python
def _concatenate_segments(self, package: dict[str, Any], segments: list[Path], output_directory: Path) -> Path
```

```text
Concatenate segments.

Args:
    package (dict[str, Any]): Compiled package consumed by the operation.
    segments (list[Path]): Segments used by this operation.
    output_directory (Path): Output directory used by this operation.

Returns:
    Path: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `SceneGenerationPipeline._produce_scene_audio`

Defined at line 470.

```python
def _produce_scene_audio(self, package: dict[str, Any], output_directory: Path) -> list[dict[str, Any]]
```

```text
Execute scene audio.

Args:
    package (dict[str, Any]): Compiled package consumed by the operation.
    output_directory (Path): Output directory used by this operation.

Returns:
    list[dict[str, Any]]: Result produced by the operation.
```

#### `SceneGenerationPipeline._generate_audio_layer`

Defined at line 503.

```python
def _generate_audio_layer(self, package: dict[str, Any], layer: dict[str, Any], layer_id: str, output_directory: Path) -> dict[str, Any]
```

```text
Generate audio layer.

Args:
    package (dict[str, Any]): Compiled package consumed by the operation.
    layer (dict[str, Any]): Layer used by this operation.
    layer_id (str): Layer id used by this operation.
    output_directory (Path): Output directory used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `SceneGenerationPipeline._mix_scene_audio`

Defined at line 537.

```python
def _mix_scene_audio(self, package: dict[str, Any], picture_track: Path, audio_layers: list[dict[str, Any]], output_directory: Path) -> Path
```

```text
Mix scene audio.

Args:
    package (dict[str, Any]): Compiled package consumed by the operation.
    picture_track (Path): Picture track used by this operation.
    audio_layers (list[dict[str, Any]]): Audio layers used by this operation.
    output_directory (Path): Output directory used by this operation.

Returns:
    Path: Result produced by the operation.
```

#### `SceneGenerationPipeline._invoke_output_tool`

Defined at line 600.

```python
def _invoke_output_tool(self, tool: dict[str, Any], payload: dict[str, Any]) -> Path
```

```text
Execute output tool.

Args:
    tool (dict[str, Any]): Tool used by this operation.
    payload (dict[str, Any]): Payload used by this operation.

Returns:
    Path: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `SceneGenerationPipeline._invoke_comfyui`

Defined at line 647.

```python
def _invoke_comfyui(self, tool: dict[str, Any], workflow: dict[str, Any], values: dict[str, Any], target_directory: Path) -> Path
```

```text
Execute comfyui.

Args:
    tool (dict[str, Any]): Tool used by this operation.
    workflow (dict[str, Any]): Workflow used by this operation.
    values (dict[str, Any]): Values used by this operation.
    target_directory (Path): Target directory used by this operation.

Returns:
    Path: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `SceneGenerationPipeline._prepare_comfyui_inputs`

Defined at line 711.

```python
def _prepare_comfyui_inputs(self, settings: dict[str, Any], values: dict[str, Any]) -> dict[str, Any]
```

```text
Prepare comfyui inputs.

Args:
    settings (dict[str, Any]): Settings used by this operation.
    values (dict[str, Any]): Values used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `SceneGenerationPipeline._apply_workflow_bindings`

Defined at line 746.

```python
def _apply_workflow_bindings(workflow_document: dict[str, Any], bindings: dict[str, Any], values: dict[str, Any]) -> None
```

```text
Apply workflow bindings.

Args:
    workflow_document (dict[str, Any]): Workflow document used by this
        operation.
    bindings (dict[str, Any]): Bindings used by this operation.
    values (dict[str, Any]): Values used by this operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `SceneGenerationPipeline._wait_for_comfyui`

Defined at line 774.

```python
def _wait_for_comfyui(self, settings: dict[str, Any], prompt_id: str) -> dict[str, Any]
```

```text
Execute for comfyui.

Args:
    settings (dict[str, Any]): Settings used by this operation.
    prompt_id (str): Prompt id used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `SceneGenerationPipeline._find_comfyui_output`

Defined at line 814.

```python
def _find_comfyui_output(settings: dict[str, Any], history: dict[str, Any], output_nodes: list[str]) -> Path
```

```text
Find comfyui output.

Args:
    settings (dict[str, Any]): Settings used by this operation.
    history (dict[str, Any]): History used by this operation.
    output_nodes (list[str]): Output nodes used by this operation.

Returns:
    Path: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `SceneGenerationPipeline._build_dry_run_plan`

Defined at line 851.

```python
def _build_dry_run_plan(self, package: dict[str, Any]) -> dict[str, Any]
```

```text
Build dry run plan.

Args:
    package (dict[str, Any]): Compiled package consumed by the operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `SceneGenerationPipeline._get_tool`

Defined at line 888.

```python
def _get_tool(package: dict[str, Any], tool_id: str) -> dict[str, Any]
```

```text
Return tool.

Args:
    package (dict[str, Any]): Compiled package consumed by the operation.
    tool_id (str): Tool id used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `SceneGenerationPipeline._get_shot_continuity`

Defined at line 909.

```python
def _get_shot_continuity(package: dict[str, Any], shot_id: str) -> dict[str, Any]
```

```text
Return shot continuity.

Args:
    package (dict[str, Any]): Compiled package consumed by the operation.
    shot_id (str): Stable identifier of the shot being processed.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `SceneGenerationPipeline._resolve_project_path`

Defined at line 923.

```python
def _resolve_project_path(self, value: str, *, require_within_project: bool = False) -> Path
```

```text
Resolve project path.

Args:
    value (str): Value inspected or transformed by the helper.
    require_within_project (bool): Require within project used by this
        operation. Defaults to ``False``.

Returns:
    Path: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `SceneGenerationPipeline._calculate_frame_count`

Defined at line 953.

```python
def _calculate_frame_count(duration_seconds: float, fps: int) -> int
```

```text
Calculate frame count.

Args:
    duration_seconds (float): Requested duration in seconds.
    fps (int): Fps used by this operation.

Returns:
    int: Result produced by the operation.
```

#### `SceneGenerationPipeline._escape_concat_path`

Defined at line 969.

```python
def _escape_concat_path(path: Path) -> str
```

```text
Execute concat path.

Args:
    path (Path): Filesystem path read or written by the operation.

Returns:
    str: Result produced by the operation.
```

#### `SceneGenerationPipeline._run_command`

Defined at line 981.

```python
def _run_command(command: list[str]) -> None
```

```text
Run command.

Args:
    command (list[str]): Command used by this operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

## `lpw.generation.prompts`

Source: [`src/lpw/generation/prompts.py`](../src/lpw/generation/prompts.py#L1)

```text
Provide prompts services for the LPW cinematic pipeline.
```

### `yaml_like_section`

Defined at line 39.

```python
def yaml_like_section(title: str, value: Any) -> str
```

```text
Execute like section.

Args:
    title (str): Title used by this operation.
    value (Any): Value inspected or transformed by the helper.

Returns:
    str: Result produced by the operation.
```

### `build_positive_prompt`

Defined at line 55.

```python
def build_positive_prompt(request: ShotRequest, context: dict[str, Any]) -> str
```

```text
Build positive prompt.

Args:
    request (ShotRequest): Typed request containing the inputs for this operation.
    context (dict[str, Any]): Resolved cinematic context used by the operation.

Returns:
    str: Result produced by the operation.
```

### `extract_negative_values`

Defined at line 106.

```python
def extract_negative_values(value: Any) -> list[str]
```

```text
Extract negative values.

Args:
    value (Any): Value inspected or transformed by the helper.

Returns:
    list[str]: Result produced by the operation.
```

### `build_negative_prompt`

Defined at line 124.

```python
def build_negative_prompt(context: dict[str, Any]) -> str
```

```text
Build negative prompt.

Args:
    context (dict[str, Any]): Resolved cinematic context used by the operation.

Returns:
    str: Result produced by the operation.
```

## `lpw.generation.render_service`

Source: [`src/lpw/generation/render_service.py`](../src/lpw/generation/render_service.py#L1)

```text
Provide render service services for the LPW cinematic pipeline.
```

### `WanRenderServiceError`

Defined at line 24.

```python
class WanRenderServiceError(RuntimeError)
```

```text
Raised when an I2V render cannot be prepared or stored safely.
```

### `parse_size`

Defined at line 28.

```python
def parse_size(size: str) -> tuple[int, int]
```

```text
Parse size.

Args:
    size (str): Size used by this operation.

Returns:
    tuple[int, int]: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

### `collect_output_descriptors`

Defined at line 49.

```python
def collect_output_descriptors(history: dict[str, Any]) -> list[dict[str, Any]]
```

```text
Collect output descriptors.

Args:
    history (dict[str, Any]): History used by this operation.

Returns:
    list[dict[str, Any]]: Result produced by the operation.
```

### `WanRenderService`

Defined at line 73.

```python
class WanRenderService
```

```text
Resolve semantic bindings and execute one Wan I2V package in ComfyUI.
```

#### `WanRenderService.__init__`

Defined at line 76.

```python
def __init__(self, client: ComfyUIClient, *, workflow_file: Path, bindings_file: Path, render_root: Path, project_root: Path = PROJECT_ROOT) -> None
```

```text
Initialize the service with its configured dependencies.

Args:
    client (ComfyUIClient): Configured integration client used for provider
        requests.
    workflow_file (Path): Workflow file used by this operation.
    bindings_file (Path): Bindings file used by this operation.
    render_root (Path): Render root used by this operation.
    project_root (Path): Root directory containing project runtime data.
        Defaults to ``PROJECT_ROOT``.
```

#### `WanRenderService.from_project_config`

Defined at line 103.

```python
def from_project_config(cls) -> WanRenderService
```

```text
Execute project config.

Returns:
    WanRenderService: Result produced by the operation.

Raises:
    WanRenderServiceError: If inputs, context, state, or provider output are invalid.
```

#### `WanRenderService.render`

Defined at line 147.

```python
async def render(self, package: WanShotPackage) -> dict[str, Any]
```

```text
Render render.

Args:
    package (WanShotPackage): Compiled package consumed by the operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    WanRenderServiceError: If inputs, context, state, or provider output are invalid.
```

### `render_wan_package`

Defined at line 255.

```python
async def render_wan_package(package: WanShotPackage) -> dict[str, Any]
```

```text
Render using the disabled-by-default project ComfyUI configuration.

Args:
    package (WanShotPackage): Compiled package consumed by the operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

## `lpw.generation.workflow_adapter`

Source: [`src/lpw/generation/workflow_adapter.py`](../src/lpw/generation/workflow_adapter.py#L1)

```text
Provide workflow adapter services for the LPW cinematic pipeline.
```

### `WorkflowBindingError`

Defined at line 13.

```python
class WorkflowBindingError(RuntimeError)
```

```text
Raised when a semantic workflow binding cannot be applied.
```

### `load_workflow`

Defined at line 17.

```python
def load_workflow(path: Path) -> dict[str, Any]
```

```text
Load workflow.

Args:
    path (Path): Filesystem path read or written by the operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    FileNotFoundError: If inputs, context, state, or provider output are invalid.
    WorkflowBindingError: If inputs, context, state, or provider output are invalid.
```

### `load_bindings`

Defined at line 43.

```python
def load_bindings(path: Path) -> dict[str, list[dict[str, str]]]
```

```text
Load bindings.

Args:
    path (Path): Filesystem path read or written by the operation.

Returns:
    dict[str, list[dict[str, str]]]: Result produced by the operation.

Raises:
    FileNotFoundError: If inputs, context, state, or provider output are invalid.
    WorkflowBindingError: If inputs, context, state, or provider output are invalid.
```

### `find_node_by_title`

Defined at line 90.

```python
def find_node_by_title(workflow: dict[str, Any], title: str) -> tuple[str, dict[str, Any]]
```

```text
Find node by title.

Args:
    workflow (dict[str, Any]): Workflow used by this operation.
    title (str): Title used by this operation.

Returns:
    tuple[str, dict[str, Any]]: Result produced by the operation.

Raises:
    WorkflowBindingError: If inputs, context, state, or provider output are invalid.
```

### `set_node_input`

Defined at line 117.

```python
def set_node_input(workflow: dict[str, Any], *, title: str, input_name: str, value: Any) -> None
```

```text
Execute node input.

Args:
    workflow (dict[str, Any]): Workflow used by this operation.
    title (str): Title used by this operation.
    input_name (str): Input name used by this operation.
    value (Any): Value inspected or transformed by the helper.

Raises:
    WorkflowBindingError: If inputs, context, state, or provider output are invalid.
```

### `apply_workflow_values`

Defined at line 144.

```python
def apply_workflow_values(workflow_template: dict[str, Any], bindings: dict[str, list[dict[str, str]]], values: dict[str, Any]) -> dict[str, Any]
```

```text
Apply semantic cinematic values to a copied ComfyUI API workflow.

Args:
    workflow_template (dict[str, Any]): Workflow template used by this operation.
    bindings (dict[str, list[dict[str, str]]]): Bindings used by this operation.
    values (dict[str, Any]): Values used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

## `lpw.generation.workflows`

Source: [`src/lpw/generation/workflows.py`](../src/lpw/generation/workflows.py#L1)

```text
Provide workflows services for the LPW cinematic pipeline.
```

### `prepare_generation_job`

Defined at line 15.

```python
def prepare_generation_job(request: ShotRequest, workflow_name: str) -> dict[str, Any]
```

```text
Load a verified ComfyUI workflow and pair it with a compiled shot.

Args:
    request (ShotRequest): Typed request containing the inputs for this operation.
    workflow_name (str): Workflow name used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

## `lpw.integrations`

Source: [`src/lpw/integrations/__init__.py`](../src/lpw/integrations/__init__.py#L1)

```text
Expose the public integrations package API for LPW.
```

## `lpw.integrations.comfyui`

Source: [`src/lpw/integrations/comfyui.py`](../src/lpw/integrations/comfyui.py#L1)

```text
Provide comfyui services for the LPW cinematic pipeline.
```

### `ComfyUIError`

Defined at line 16.

```python
class ComfyUIError(RuntimeError)
```

```text
Base error for ComfyUI API failures.
```

### `ComfyUIExecutionError`

Defined at line 20.

```python
class ComfyUIExecutionError(ComfyUIError)
```

```text
Raised when a ComfyUI workflow fails or times out.
```

### `ComfyUIClient`

Defined at line 24.

```python
class ComfyUIClient
```

```text
Async client for ComfyUI's built-in HTTP API.
```

#### `ComfyUIClient.__init__`

Defined at line 27.

```python
def __init__(self, base_url: str = 'http://127.0.0.1:8188', *, request_timeout_seconds: float = 60.0, poll_interval_seconds: float = 1.0) -> None
```

```text
Initialize the service with its configured dependencies.

Args:
    base_url (str): Base url used by this operation. Defaults to
        ``'http://127.0.0.1:8188'``.
    request_timeout_seconds (float): Request timeout seconds used by this
        operation. Defaults to ``60.0``.
    poll_interval_seconds (float): Poll interval seconds used by this operation.
        Defaults to ``1.0``.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

#### `ComfyUIClient.health_check`

Defined at line 53.

```python
async def health_check(self) -> dict[str, Any]
```

```text
Execute check.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `ComfyUIClient.upload_image`

Defined at line 64.

```python
async def upload_image(self, image_path: Path) -> str
```

```text
Upload image.

Args:
    image_path (Path): Image path used by this operation.

Returns:
    str: Result produced by the operation.

Raises:
    FileNotFoundError: If inputs, context, state, or provider output are invalid.
    ComfyUIError: If inputs, context, state, or provider output are invalid.
```

#### `ComfyUIClient.queue_workflow`

Defined at line 96.

```python
async def queue_workflow(self, workflow: dict[str, Any], client_id: str | None = None) -> str
```

```text
Queue workflow.

Args:
    workflow (dict[str, Any]): Workflow used by this operation.
    client_id (str | None): Client id used by this operation. Defaults to
        ``None``.

Returns:
    str: Result produced by the operation.

Raises:
    ComfyUIExecutionError: If inputs, context, state, or provider output are invalid.
    ComfyUIError: If inputs, context, state, or provider output are invalid.
```

#### `ComfyUIClient.get_history`

Defined at line 127.

```python
async def get_history(self, prompt_id: str) -> dict[str, Any] | None
```

```text
Return history.

Args:
    prompt_id (str): Prompt id used by this operation.

Returns:
    dict[str, Any] | None: Result produced by the operation.
```

#### `ComfyUIClient.execute`

Defined at line 143.

```python
async def execute(self, workflow: dict[str, Any], *, timeout_seconds: float = 3600) -> tuple[str, dict[str, Any]]
```

```text
Execute execute.

Args:
    workflow (dict[str, Any]): Workflow used by this operation.
    timeout_seconds (float): Timeout seconds used by this operation. Defaults to
        ``3600``.

Returns:
    tuple[str, dict[str, Any]]: Result produced by the operation.

Raises:
    ComfyUIExecutionError: If inputs, context, state, or provider output are invalid.
```

#### `ComfyUIClient.download_output`

Defined at line 176.

```python
async def download_output(self, descriptor: dict[str, Any], destination: Path) -> Path
```

```text
Download output.

Args:
    descriptor (dict[str, Any]): Descriptor used by this operation.
    destination (Path): Destination path for the transformed asset.

Returns:
    Path: Result produced by the operation.

Raises:
    ComfyUIError: If inputs, context, state, or provider output are invalid.
```

#### `ComfyUIClient._json_object`

Defined at line 210.

```python
def _json_object(value: Any, label: str) -> dict[str, Any]
```

```text
Execute object.

Args:
    value (Any): Value inspected or transformed by the helper.
    label (str): Label used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ComfyUIError: If inputs, context, state, or provider output are invalid.
```

## `lpw.mcp`

Source: [`src/lpw/mcp/__init__.py`](../src/lpw/mcp/__init__.py#L1)

```text
MCP transport adapters for the domain services.
```

## `lpw.mcp.resources`

Source: [`src/lpw/mcp/resources.py`](../src/lpw/mcp/resources.py#L1)

```text
Provide MCP resource handlers for cinematic context and runtime state.
```

### `studio_context`

Defined at line 28.

```python
def studio_context() -> str
```

```text
Execute context.

Returns:
    str: Result produced by the operation.
```

### `studio_profile`

Defined at line 41.

```python
def studio_profile(profile: str) -> str
```

```text
Execute profile.

Args:
    profile (str): Profile used by this operation.

Returns:
    str: Result produced by the operation.
```

### `editing_models_context`

Defined at line 57.

```python
def editing_models_context() -> str
```

```text
Return configured local editing roles without loading model artifacts.

Returns:
    str: Result produced by the operation.
```

### `wan22_context`

Defined at line 71.

```python
def wan22_context() -> str
```

```text
Return the protected non-animation Wan 2.2 setup.

Returns:
    str: YAML containing disabled T2V, I2V, TI2V, and S2V declarations,
    external-artifact policy, and the explicit Animate exclusion.
```

### `project_context`

Defined at line 86.

```python
def project_context(project_id: str) -> str
```

```text
Execute context.

Args:
    project_id (str): Stable identifier of the project whose context is used.

Returns:
    str: Result produced by the operation.
```

### `character_context`

Defined at line 100.

```python
def character_context(project_id: str, character_id: str) -> str
```

```text
Execute context.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    character_id (str): Stable identifier of the character.

Returns:
    str: Result produced by the operation.
```

### `location_context`

Defined at line 117.

```python
def location_context(project_id: str, location_id: str) -> str
```

```text
Execute context.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    location_id (str): Stable identifier of the location.

Returns:
    str: Result produced by the operation.
```

### `project_audio_context`

Defined at line 134.

```python
def project_audio_context(project_id: str) -> str
```

```text
Execute audio context.

Args:
    project_id (str): Stable identifier of the project whose context is used.

Returns:
    str: Result produced by the operation.
```

### `character_voice_context`

Defined at line 146.

```python
def character_voice_context(project_id: str, character_id: str) -> str
```

```text
Execute voice context.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    character_id (str): Stable identifier of the character.

Returns:
    str: Result produced by the operation.
```

### `voice_production_context`

Defined at line 161.

```python
def voice_production_context(project_id: str) -> str
```

```text
Execute production context.

Args:
    project_id (str): Stable identifier of the project whose context is used.

Returns:
    str: Result produced by the operation.
```

### `sound_effects_context`

Defined at line 178.

```python
def sound_effects_context(project_id: str) -> str
```

```text
Execute effects context.

Args:
    project_id (str): Stable identifier of the project whose context is used.

Returns:
    str: Result produced by the operation.
```

### `music_context`

Defined at line 193.

```python
def music_context(project_id: str) -> str
```

```text
Execute context.

Args:
    project_id (str): Stable identifier of the project whose context is used.

Returns:
    str: Result produced by the operation.
```

### `editing_context`

Defined at line 206.

```python
def editing_context(project_id: str) -> str
```

```text
Execute context.

Args:
    project_id (str): Stable identifier of the project whose context is used.

Returns:
    str: Result produced by the operation.
```

### `post_editing_context`

Defined at line 226.

```python
def post_editing_context(project_id: str) -> str
```

```text
Execute editing context.

Args:
    project_id (str): Stable identifier of the project whose context is used.

Returns:
    str: Result produced by the operation.
```

### `export_context`

Defined at line 243.

```python
def export_context(project_id: str) -> str
```

```text
Execute context.

Args:
    project_id (str): Stable identifier of the project whose context is used.

Returns:
    str: Result produced by the operation.
```

### `continuity_context`

Defined at line 260.

```python
def continuity_context(project_id: str) -> str
```

```text
Execute context.

Args:
    project_id (str): Stable identifier of the project whose context is used.

Returns:
    str: Result produced by the operation.
```

### `scene_editing_context`

Defined at line 283.

```python
def scene_editing_context(project_id: str, scene_id: str) -> str
```

```text
Execute editing context.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    str: Result produced by the operation.
```

### `prepared_editing_package`

Defined at line 301.

```python
def prepared_editing_package(project_id: str, scene_id: str) -> str
```

```text
Execute editing package.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    str: Result produced by the operation.
```

### `scene_context`

Defined at line 317.

```python
def scene_context(story_id: str, scene_id: str) -> str
```

```text
Return a story-linked scene with all referenced shots expanded.

Args:
    story_id (str): Stable identifier of the story to load or compile.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    str: Result produced by the operation.
```

### `inherited_scene_context`

Defined at line 332.

```python
def inherited_scene_context(project_id: str, scene_id: str) -> str
```

```text
Execute scene context.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    str: Result produced by the operation.
```

### `inherited_shot_context`

Defined at line 358.

```python
def inherited_shot_context(project_id: str, shot_id: str) -> str
```

```text
Execute shot context.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    shot_id (str): Stable identifier of the shot being processed.

Returns:
    str: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

### `segment_end_state`

Defined at line 384.

```python
def segment_end_state(segment_id: str) -> str
```

```text
Execute end state.

Args:
    segment_id (str): Stable identifier of the chained video segment.

Returns:
    str: Result produced by the operation.
```

### `runtime_audio_state`

Defined at line 400.

```python
def runtime_audio_state(scene_id: str) -> str
```

```text
Execute audio state.

Args:
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    str: Result produced by the operation.
```

### `runtime_editing_state`

Defined at line 416.

```python
def runtime_editing_state(scene_id: str) -> str
```

```text
Execute editing state.

Args:
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    str: Result produced by the operation.
```

## `lpw.mcp.server`

Source: [`src/lpw/mcp/server.py`](../src/lpw/mcp/server.py#L1)

```text
Expose the compatibility entry point for the LPW MCP server.
```

### `main`

Defined at line 115.

```python
def main() -> None
```

```text
Execute main.
```

## `lpw.mcp.tools`

Source: [`src/lpw/mcp/tools.py`](../src/lpw/mcp/tools.py#L1)

```text
Provide MCP tool handlers for LPW production workflows.
```

### `compile_scene_context`

Defined at line 44.

```python
def compile_scene_context(story_id: str, scene_id: str) -> dict[str, Any]
```

```text
Compile project, story, scene, and referenced shot YAML into one object.

Args:
    story_id (str): Stable identifier of the story to load or compile.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `get_scene_summary`

Defined at line 58.

```python
def get_scene_summary(story_id: str, scene_id: str) -> dict[str, Any]
```

```text
Summarize duration, location, shot count, and generation modes.

Args:
    story_id (str): Stable identifier of the story to load or compile.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `build_scene_generation_plan`

Defined at line 72.

```python
def build_scene_generation_plan(story_id: str, scene_id: str) -> list[dict[str, Any]]
```

```text
Build an ordered generation plan from the scene's referenced shots.

Args:
    story_id (str): Stable identifier of the story to load or compile.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    list[dict[str, Any]]: Result produced by the operation.
```

### `plan_scene_production`

Defined at line 88.

```python
def plan_scene_production(story_id: str, scene_id: str) -> dict[str, Any]
```

```text
Resolve tools and workflows for a scene without invoking external services.

Args:
    story_id (str): Stable identifier of the story to load or compile.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `create_render_attempt`

Defined at line 106.

```python
def create_render_attempt(story_id: str, scene_id: str, shot_id: str, video_file: str, dialogue_file: str | None = None) -> dict[str, Any]
```

```text
Store a new immutable render attempt and its compiled context snapshot.

Args:
    story_id (str): Stable identifier of the story to load or compile.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    video_file (str): Path to the video file associated with the operation.
    dialogue_file (str | None): Dialogue file used by this operation. Defaults to
        ``None``.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `validate_render_attempt`

Defined at line 132.

```python
def validate_render_attempt(attempt_directory: str) -> dict[str, Any]
```

```text
Run FFprobe and FFmpeg validation for a stored attempt.

Args:
    attempt_directory (str): Attempt directory used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `review_render_attempt`

Defined at line 145.

```python
def review_render_attempt(attempt_directory: str, reviewer: str, results: dict[str, dict[str, str]]) -> dict[str, Any]
```

```text
Record human semantic-review results for a stored attempt.

Args:
    attempt_directory (str): Attempt directory used by this operation.
    reviewer (str): Human reviewer identity recorded with the decision.
    results (dict[str, dict[str, str]]): Results used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `approve_render_attempt`

Defined at line 166.

```python
def approve_render_attempt(attempt_directory: str, reviewer: str, notes: str = '', scores: dict[str, int] | None = None, continuity: dict[str, Any] | None = None) -> dict[str, Any]
```

```text
Approve a validated attempt when all configured gates pass.

Args:
    attempt_directory (str): Attempt directory used by this operation.
    reviewer (str): Human reviewer identity recorded with the decision.
    notes (str): Optional review or production notes. Defaults to ``''``.
    scores (dict[str, int] | None): Named review scores used by approval thresholds.
        Defaults to ``None``.
    continuity (dict[str, Any] | None): Continuity state approved for subsequent
        production. Defaults to ``None``.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `reject_render_attempt`

Defined at line 193.

```python
def reject_render_attempt(attempt_directory: str, reviewer: str, reason: str) -> dict[str, Any]
```

```text
Record an immutable rejection decision without deleting the attempt.

Args:
    attempt_directory (str): Attempt directory used by this operation.
    reviewer (str): Human reviewer identity recorded with the decision.
    reason (str): Reason used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `create_scene_release`

Defined at line 214.

```python
def create_scene_release(story_id: str, scene_id: str, final_video: str) -> dict[str, Any]
```

```text
Create a versioned release after every scene shot has an approval.

Args:
    story_id (str): Stable identifier of the story to load or compile.
    scene_id (str): Stable identifier of the scene being processed.
    final_video (str): Final video used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `_shot_request`

Defined at line 235.

```python
def _shot_request(project_id: str, shot_id: str, scene_id: str, character_ids: list[str], location_id: str, shot_type: str, action: str, framing: str, lens: str, camera_height: str, camera_movement: str, emotional_tone: str = '', dialogue_audio: str | None = None, start_frame: str | None = None, performer_video: str | None = None, duration_seconds: int = 5, seed: int | None = None) -> ShotRequest
```

```text
Execute request.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    shot_id (str): Stable identifier of the shot being processed.
    scene_id (str): Stable identifier of the scene being processed.
    character_ids (list[str]): Character identifiers whose context should be
        included.
    location_id (str): Stable identifier of the location.
    shot_type (str): Shot type used by this operation.
    action (str): Action used by this operation.
    framing (str): Framing used by this operation.
    lens (str): Lens used by this operation.
    camera_height (str): Camera height used by this operation.
    camera_movement (str): Camera movement used by this operation.
    emotional_tone (str): Emotional tone used by this operation. Defaults to ``''``.
    dialogue_audio (str | None): Dialogue audio used by this operation. Defaults to
        ``None``.
    start_frame (str | None): Start frame used by this operation. Defaults to
        ``None``.
    performer_video (str | None): Performer video used by this operation. Defaults
        to ``None``.
    duration_seconds (int): Requested duration in seconds. Defaults to ``5``.
    seed (int | None): Optional deterministic generation seed. Defaults to ``None``.

Returns:
    ShotRequest: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

### `inspect_project_context`

Defined at line 310.

```python
def inspect_project_context(project_id: str, character_ids: list[str] | None = None, location_id: str | None = None) -> dict[str, Any]
```

```text
Inspect the complete normalized context and its deterministic hash.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    character_ids (list[str] | None): Character identifiers whose context should be
        included. Defaults to ``None``.
    location_id (str | None): Stable identifier of the location. Defaults to
        ``None``.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `compile_wan_shot`

Defined at line 333.

```python
def compile_wan_shot(project_id: str, shot_id: str, scene_id: str, character_ids: list[str], location_id: str, shot_type: str, action: str, framing: str, lens: str, camera_height: str, camera_movement: str, emotional_tone: str = '', dialogue_audio: str | None = None, start_frame: str | None = None, performer_video: str | None = None, duration_seconds: int = 5, seed: int | None = None) -> dict[str, Any]
```

```text
Compile wan shot.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    shot_id (str): Stable identifier of the shot being processed.
    scene_id (str): Stable identifier of the scene being processed.
    character_ids (list[str]): Character identifiers whose context should be
        included.
    location_id (str): Stable identifier of the location.
    shot_type (str): Shot type used by this operation.
    action (str): Action used by this operation.
    framing (str): Framing used by this operation.
    lens (str): Lens used by this operation.
    camera_height (str): Camera height used by this operation.
    camera_movement (str): Camera movement used by this operation.
    emotional_tone (str): Emotional tone used by this operation. Defaults to ``''``.
    dialogue_audio (str | None): Dialogue audio used by this operation. Defaults to
        ``None``.
    start_frame (str | None): Start frame used by this operation. Defaults to
        ``None``.
    performer_video (str | None): Performer video used by this operation. Defaults
        to ``None``.
    duration_seconds (int): Requested duration in seconds. Defaults to ``5``.
    seed (int | None): Optional deterministic generation seed. Defaults to ``None``.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `compile_dialogue`

Defined at line 384.

```python
def compile_dialogue(project_id: str, scene_id: str, shot_id: str, character_id: str, text: str, emotion: str, intensity: float = 0.5, language: str = 'Persian') -> dict[str, Any]
```

```text
Compile dialogue.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    character_id (str): Stable identifier of the character.
    text (str): Exact text content used by the operation.
    emotion (str): Requested emotional delivery.
    intensity (float): Normalized requested performance intensity. Defaults to
        ``0.5``.
    language (str): Language used for dialogue and pronunciation rules. Defaults to
        ``'Persian'``.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `compile_cinematic_shot`

Defined at line 414.

```python
def compile_cinematic_shot(project_id: str, shot_id: str, scene_id: str, character_ids: list[str], location_id: str, shot_type: str, action: str, framing: str, lens: str, camera_height: str, camera_movement: str, emotional_tone: str = '', dialogue_character_id: str | None = None, dialogue_text: str | None = None, dialogue_emotion: str = 'neutral', dialogue_intensity: float = 0.5, dialogue_audio: str | None = None, sound_effects: list[dict[str, Any]] | None = None, start_frame: str | None = None, performer_video: str | None = None, duration_seconds: int = 5, seed: int | None = None) -> dict[str, Any]
```

```text
Compile cinematic shot.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    shot_id (str): Stable identifier of the shot being processed.
    scene_id (str): Stable identifier of the scene being processed.
    character_ids (list[str]): Character identifiers whose context should be
        included.
    location_id (str): Stable identifier of the location.
    shot_type (str): Shot type used by this operation.
    action (str): Action used by this operation.
    framing (str): Framing used by this operation.
    lens (str): Lens used by this operation.
    camera_height (str): Camera height used by this operation.
    camera_movement (str): Camera movement used by this operation.
    emotional_tone (str): Emotional tone used by this operation. Defaults to ``''``.
    dialogue_character_id (str | None): Dialogue character id used by this
        operation. Defaults to ``None``.
    dialogue_text (str | None): Dialogue text used by this operation. Defaults to
        ``None``.
    dialogue_emotion (str): Dialogue emotion used by this operation. Defaults to
        ``'neutral'``.
    dialogue_intensity (float): Dialogue intensity used by this operation. Defaults
        to ``0.5``.
    dialogue_audio (str | None): Dialogue audio used by this operation. Defaults to
        ``None``.
    sound_effects (list[dict[str, Any]] | None): Sound effects used by this
        operation. Defaults to ``None``.
    start_frame (str | None): Start frame used by this operation. Defaults to
        ``None``.
    performer_video (str | None): Performer video used by this operation. Defaults
        to ``None``.
    duration_seconds (int): Requested duration in seconds. Defaults to ``5``.
    seed (int | None): Optional deterministic generation seed. Defaults to ``None``.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ValueError: If inputs, context, state, or provider output are invalid.
```

### `generate_cinematic_video`

Defined at line 513.

```python
def generate_cinematic_video(workflow_name: str, project_id: str, shot_id: str, scene_id: str, character_ids: list[str], location_id: str, shot_type: str, action: str, framing: str, lens: str, camera_height: str, camera_movement: str, emotional_tone: str = '', dialogue_audio: str | None = None, start_frame: str | None = None, performer_video: str | None = None, duration_seconds: int = 5, seed: int | None = None) -> dict[str, Any]
```

```text
Generate cinematic video.

Args:
    workflow_name (str): Workflow name used by this operation.
    project_id (str): Stable identifier of the project whose context is used.
    shot_id (str): Stable identifier of the shot being processed.
    scene_id (str): Stable identifier of the scene being processed.
    character_ids (list[str]): Character identifiers whose context should be
        included.
    location_id (str): Stable identifier of the location.
    shot_type (str): Shot type used by this operation.
    action (str): Action used by this operation.
    framing (str): Framing used by this operation.
    lens (str): Lens used by this operation.
    camera_height (str): Camera height used by this operation.
    camera_movement (str): Camera movement used by this operation.
    emotional_tone (str): Emotional tone used by this operation. Defaults to ``''``.
    dialogue_audio (str | None): Dialogue audio used by this operation. Defaults to
        ``None``.
    start_frame (str | None): Start frame used by this operation. Defaults to
        ``None``.
    performer_video (str | None): Performer video used by this operation. Defaults
        to ``None``.
    duration_seconds (int): Requested duration in seconds. Defaults to ``5``.
    seed (int | None): Optional deterministic generation seed. Defaults to ``None``.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `render_wan_i2v_shot`

Defined at line 567.

```python
async def render_wan_i2v_shot(project_id: str, scene_id: str, shot_id: str, character_ids: list[str], location_id: str, action: str, start_frame: str, framing: str = 'medium shot', lens: str = '50mm cinema lens', camera_height: str = 'eye level', camera_movement: str = 'locked camera', emotional_tone: str = '', duration_seconds: int = 5, seed: int | None = None) -> dict[str, Any]
```

```text
Compile and render one context-enforced Wan 2.2 I2V shot.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    character_ids (list[str]): Character identifiers whose context should be
        included.
    location_id (str): Stable identifier of the location.
    action (str): Action used by this operation.
    start_frame (str): Start frame used by this operation.
    framing (str): Framing used by this operation. Defaults to ``'medium shot'``.
    lens (str): Lens used by this operation. Defaults to ``'50mm cinema lens'``.
    camera_height (str): Camera height used by this operation. Defaults to ``'eye
        level'``.
    camera_movement (str): Camera movement used by this operation. Defaults to
        ``'locked camera'``.
    emotional_tone (str): Emotional tone used by this operation. Defaults to ``''``.
    duration_seconds (int): Requested duration in seconds. Defaults to ``5``.
    seed (int | None): Optional deterministic generation seed. Defaults to ``None``.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `create_edit_plan`

Defined at line 628.

```python
def create_edit_plan(project_id: str, shot_metadata_files: list[str], reference_shot_id: str, require_existing_sources: bool = False) -> dict[str, Any]
```

```text
Create edit plan.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    shot_metadata_files (list[str]): Shot metadata files used by this operation.
    reference_shot_id (str): Reference shot id used by this operation.
    require_existing_sources (bool): Require existing sources used by this
        operation. Defaults to ``False``.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `validate_edit_plan`

Defined at line 654.

```python
def validate_edit_plan(edit_plan: dict[str, Any]) -> dict[str, list[str]]
```

```text
Validate edit plan.

Args:
    edit_plan (dict[str, Any]): Edit plan used by this operation.

Returns:
    dict[str, list[str]]: Result produced by the operation.
```

### `edit_cinematic_sequence`

Defined at line 666.

```python
def edit_cinematic_sequence(project_id: str, scene_id: str, shot_metadata_files: list[str], reference_shot_id: str, output_path: str) -> dict[str, Any]
```

```text
Execute cinematic sequence.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_metadata_files (list[str]): Shot metadata files used by this operation.
    reference_shot_id (str): Reference shot id used by this operation.
    output_path (str): Destination path for the generated output.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `save_continuity_state`

Defined at line 690.

```python
def save_continuity_state(project_id: str, scene_id: str, edit_version: int, last_shot: dict[str, Any]) -> dict[str, str]
```

```text
Save continuity state.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    edit_version (int): Positive version number of the edit.
    last_shot (dict[str, Any]): Last shot used by this operation.

Returns:
    dict[str, str]: Result produced by the operation.
```

### `finalize_cinematic_scene`

Defined at line 713.

```python
def finalize_cinematic_scene(project_id: str, scene_id: str, edit_version: int = 1) -> dict[str, Any]
```

```text
Validate, assemble, mix, export, and commit an approved scene.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    edit_version (int): Positive version number of the edit. Defaults to ``1``.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `approve_wan_render`

Defined at line 736.

```python
def approve_wan_render(render_directory: str, reviewer: str, scores: dict[str, int], continuity: dict[str, Any], review_notes: list[str] | None = None) -> dict[str, Any]
```

```text
Record immutable scored approval metadata for a completed Wan render.

Args:
    render_directory (str): Render directory used by this operation.
    reviewer (str): Human reviewer identity recorded with the decision.
    scores (dict[str, int]): Named review scores used by approval thresholds.
    continuity (dict[str, Any]): Continuity state approved for subsequent
        production.
    review_notes (list[str] | None): Structured or textual notes supplied by the
        reviewer. Defaults to ``None``.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `prepare_automated_edit`

Defined at line 767.

```python
def prepare_automated_edit(project_id: str, scene_id: str) -> dict[str, Any]
```

```text
Validate approved media and create an immutable prepared package.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `automate_scene_edit`

Defined at line 783.

```python
def automate_scene_edit(project_id: str, scene_id: str, prepared_package_version: int) -> dict[str, Any]
```

```text
Select takes, build an EDL, validate adjacency, and render a preview.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    prepared_package_version (int): Version of the prepared editing package.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `approve_edit_preview`

Defined at line 804.

```python
def approve_edit_preview(project_id: str, scene_id: str, edit_version: int, reviewer: str, notes: list[dict[str, Any]] | None = None) -> dict[str, Any]
```

```text
Approve one immutable automated-edit preview version.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    edit_version (int): Positive version number of the edit.
    reviewer (str): Human reviewer identity recorded with the decision.
    notes (list[dict[str, Any]] | None): Optional review or production notes.
        Defaults to ``None``.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `post_edit_scene`

Defined at line 830.

```python
def post_edit_scene(project_id: str, scene_id: str, approved_edit_version: int, review_notes: list[dict[str, Any]]) -> dict[str, Any]
```

```text
Compile review changes or finalize an unchanged approved edit.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    approved_edit_version (int): Version of the edit approved for post-production.
    review_notes (list[dict[str, Any]]): Structured or textual notes supplied by the
        reviewer.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `resolve_context_chain`

Defined at line 854.

```python
def resolve_context_chain(project_id: str, scene_id: str, shot_id: str) -> dict[str, Any]
```

```text
Resolve the pinned studio-to-shot context chain and return its hash.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `validate_context_chain`

Defined at line 873.

```python
def validate_context_chain(project_id: str, scene_id: str, shot_id: str) -> dict[str, Any]
```

```text
Validate pinned versions, inheritance, and immutable context fields.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `extend_cinematic_shot`

Defined at line 892.

```python
def extend_cinematic_shot(project_id: str, scene_id: str, shot_id: str, target_duration_seconds: float, segment_duration_seconds: float = 4.0) -> dict[str, Any]
```

```text
Create a versioned short-segment extension plan without invoking models.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    target_duration_seconds (float): Total desired duration of the extended shot.
    segment_duration_seconds (float): Maximum duration of each generated segment.
        Defaults to ``4.0``.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `compile_segment_context`

Defined at line 922.

```python
def compile_segment_context(project_id: str, scene_id: str, shot_id: str, extension_version: int, segment_id: str) -> dict[str, Any]
```

```text
Compile a segment after its predecessor has an approved end state.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    extension_version (int): Positive version number of the shot extension.
    segment_id (str): Stable identifier of the chained video segment.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `approve_segment`

Defined at line 947.

```python
def approve_segment(project_id: str, scene_id: str, shot_id: str, extension_version: int, segment_id: str, generated_video_path: str, approved_end_frame_path: str, generated_frames: int, approved_end_frame: int, continuity_delta: dict[str, Any], analysis: dict[str, Any], reviewer: str) -> dict[str, Any]
```

```text
Approve real segment artifacts and commit their runtime continuity delta.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    extension_version (int): Positive version number of the shot extension.
    segment_id (str): Stable identifier of the chained video segment.
    generated_video_path (str): Path to the real video returned by a provider.
    approved_end_frame_path (str): Path to the reviewed stable segment end frame.
    generated_frames (int): Total number of frames produced by the provider.
    approved_end_frame (int): Index of the reviewed stable final frame.
    continuity_delta (dict[str, Any]): Small state delta produced by an approved
        segment.
    analysis (dict[str, Any]): Structured provider or validation analysis results.
    reviewer (str): Human reviewer identity recorded with the decision.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `prepare_exact_dialogue`

Defined at line 998.

```python
def prepare_exact_dialogue(project_id: str, episode_id: str, scene_id: str, shot_id: str, character_id: str, exact_dialogue: str, language: str, primary_emotion: str, ending_emotion: str | None = None, emotional_intensity: float = 0.4, speaking_speed: str = 'slow-to-moderate', important_words: list[str] | None = None, pronunciation_notes: list[str] | None = None, pause_instructions: list[str] | None = None, audience_response_pause_seconds: float = 0, gesture_guidance: str | None = None, facial_expression_guidance: str | None = None) -> dict[str, Any]
```

```text
Prepare an exact clean-dialogue package; no voice provider is invoked.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    episode_id (str): Stable identifier of the episode being produced.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    character_id (str): Stable identifier of the character.
    exact_dialogue (str): Exact approved words that must be spoken once.
    language (str): Language used for dialogue and pronunciation rules.
    primary_emotion (str): Primary emotional direction for the performance.
    ending_emotion (str | None): Emotional direction at the end of the line.
        Defaults to ``None``.
    emotional_intensity (float): Child-safe normalized emotional intensity. Defaults
        to ``0.4``.
    speaking_speed (str): Speaking speed used by this operation. Defaults to
        ``'slow-to-moderate'``.
    important_words (list[str] | None): Important words used by this operation.
        Defaults to ``None``.
    pronunciation_notes (list[str] | None): Pronunciation notes used by this
        operation. Defaults to ``None``.
    pause_instructions (list[str] | None): Pause instructions used by this
        operation. Defaults to ``None``.
    audience_response_pause_seconds (float): Audience response pause seconds used by
        this operation. Defaults to ``0``.
    gesture_guidance (str | None): Gesture guidance used by this operation. Defaults
        to ``None``.
    facial_expression_guidance (str | None): Facial expression guidance used by this
        operation. Defaults to ``None``.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `lock_exact_dialogue`

Defined at line 1072.

```python
def lock_exact_dialogue(project_id: str, episode_id: str, scene_id: str, shot_id: str, character_id: str, dialogue_version: int, audio_path: str, verified_transcript: str, reviewer: str, review: dict[str, bool]) -> dict[str, Any]
```

```text
Lock existing dialogue only after exact-text and safety review passes.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    episode_id (str): Stable identifier of the episode being produced.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    character_id (str): Stable identifier of the character.
    dialogue_version (int): Positive version number of the dialogue package.
    audio_path (str): Path to the audio asset being processed.
    verified_transcript (str): Reviewed transcript expected to match the dialogue
        exactly.
    reviewer (str): Human reviewer identity recorded with the decision.
    review (dict[str, bool]): Required review checks and their pass/fail values.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `compile_dialogue_lip_sync_context`

Defined at line 1117.

```python
def compile_dialogue_lip_sync_context(project_id: str, episode_id: str, scene_id: str, shot_id: str, character_id: str, dialogue_version: int) -> dict[str, Any]
```

```text
Compile video performance context from locked authoritative dialogue.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    episode_id (str): Stable identifier of the episode being produced.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    character_id (str): Stable identifier of the character.
    dialogue_version (int): Positive version number of the dialogue package.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `compile_sound_cue_sheet`

Defined at line 1149.

```python
def compile_sound_cue_sheet(project_id: str, scene_id: str, shot_id: str, location_id: str, cues: list[dict[str, Any]], dialogue_present: bool, participation_pause: bool = False) -> dict[str, Any]
```

```text
Compile visible-action, Foley, and learning cues separately from dialogue.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    location_id (str): Stable identifier of the location.
    cues (list[dict[str, Any]]): Structured audio or music cue definitions.
    dialogue_present (bool): Whether dialogue must have priority in the mix.
    participation_pause (bool): Whether the cue overlaps a child-response pause.
        Defaults to ``False``.

Returns:
    dict[str, Any]: Result produced by the operation.
```

### `compile_music_cue_sheet`

Defined at line 1185.

```python
def compile_music_cue_sheet(project_id: str, episode_id: str, scene_id: str, cues: list[dict[str, Any]], dialogue_present: bool, target_age: str = 'two-to-five') -> dict[str, Any]
```

```text
Compile theme continuity, dialogue ducking, and participation pauses.

Args:
    project_id (str): Stable identifier of the project whose context is used.
    episode_id (str): Stable identifier of the episode being produced.
    scene_id (str): Stable identifier of the scene being processed.
    cues (list[dict[str, Any]]): Structured audio or music cue definitions.
    dialogue_present (bool): Whether dialogue must have priority in the mix.
    target_age (str): Audience age range used by safety rules. Defaults to ``'two-
        to-five'``.

Returns:
    dict[str, Any]: Result produced by the operation.
```

## `lpw.models`

Source: [`src/lpw/models.py`](../src/lpw/models.py#L1)

```text
Define typed request and package models used across LPW.
```

### `ShotRequest`

Defined at line 18.

```python
class ShotRequest
```

```text
Represent ShotRequest behavior in the LPW pipeline.

Attributes:
    project_id (str): Stored project id value.
    shot_id (str): Stored shot id value.
    scene_id (str): Stored scene id value.
    character_ids (list[str]): Stored character ids value.
    location_id (str): Stored location id value.
    shot_type (ShotType): Stored shot type value.
    action (str): Stored action value.
    framing (str): Stored framing value.
    lens (str): Stored lens value.
    camera_height (str): Stored camera height value.
    camera_movement (str): Stored camera movement value.
    emotional_tone (str): Stored emotional tone value.
    dialogue_audio (str | None): Stored dialogue audio value.
    start_frame (str | None): Stored start frame value.
    performer_video (str | None): Optional pose-conditioning video for S2V; it
        never enables Wan Animate.
    duration_seconds (int): Stored duration seconds value.
    seed (int | None): Stored seed value.
```

### `WanShotPackage`

Defined at line 61.

```python
class WanShotPackage
```

```text
Represent WanShotPackage behavior in the LPW pipeline.

Attributes:
    project_id (str): Stored project id value.
    scene_id (str): Stored scene id value.
    shot_id (str): Stored shot id value.
    model (str): Stored model value.
    task (str): Stored task value.
    prompt (str): Stored prompt value.
    negative_prompt (str): Stored negative prompt value.
    primary_reference_image (str | None): Stored primary reference image value.
    supporting_references (list[str]): Stored supporting references value.
    audio_path (str | None): Stored audio path value.
    performer_video (str | None): Stored performer video value.
    size (str): Stored size value.
    frame_rate (int): Stored frame rate value.
    duration_seconds (int): Stored duration seconds value.
    frame_count (int): Stored frame count value.
    seed (int): Stored seed value.
    context_hash (str): Stored context hash value.
    package_hash (str): Stored package hash value.
```

### `DialogueRequest`

Defined at line 105.

```python
class DialogueRequest
```

```text
Represent DialogueRequest behavior in the LPW pipeline.

Attributes:
    project_id (str): Stored project id value.
    scene_id (str): Stored scene id value.
    shot_id (str): Stored shot id value.
    character_id (str): Stored character id value.
    text (str): Stored text value.
    emotion (str): Stored emotion value.
    intensity (float): Stored intensity value.
    language (str): Stored language value.
```

## `lpw.utils`

Source: [`src/lpw/utils/__init__.py`](../src/lpw/utils/__init__.py#L1)

```text
Small, dependency-light helpers shared by domain modules.
```

## `lpw.utils.files`

Source: [`src/lpw/utils/files.py`](../src/lpw/utils/files.py#L1)

```text
Provide files services for the LPW cinematic pipeline.
```

### `load_yaml`

Defined at line 19.

```python
def load_yaml(path: Path, *, required: bool = True) -> dict[str, Any]
```

```text
Load a YAML mapping, returning an empty mapping for optional files.

Args:
    path (Path): Filesystem path read or written by the operation.
    required (bool): Whether absence of the requested file is an error. Defaults to
        ``True``.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    ContextNotFoundError: If inputs, context, state, or provider output are invalid.
    InvalidContextDataError: If inputs, context, state, or provider output are invalid.
```

### `load_json`

Defined at line 50.

```python
def load_json(path: Path) -> dict[str, Any]
```

```text
Load json.

Args:
    path (Path): Filesystem path read or written by the operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    FileNotFoundError: If inputs, context, state, or provider output are invalid.
    ValueError: If inputs, context, state, or provider output are invalid.
```

### `atomic_write_yaml`

Defined at line 71.

```python
def atomic_write_yaml(path: Path, content: dict[str, Any]) -> None
```

```text
Atomically persist generated runtime state.

Args:
    path (Path): Filesystem path read or written by the operation.
    content (dict[str, Any]): Structured content written to the destination.
```

### `atomic_write_json`

Defined at line 95.

```python
def atomic_write_json(path: Path, content: Any) -> None
```

```text
Atomically persist generated JSON metadata.

Args:
    path (Path): Filesystem path read or written by the operation.
    content (Any): Structured content written to the destination.
```

## `lpw.utils.hashing`

Source: [`src/lpw/utils/hashing.py`](../src/lpw/utils/hashing.py#L1)

```text
Provide hashing services for the LPW cinematic pipeline.
```

### `stable_hash`

Defined at line 10.

```python
def stable_hash(value: Any, *, length: int = 16) -> str
```

```text
Execute hash.

Args:
    value (Any): Value inspected or transformed by the helper.
    length (int): Length used by this operation. Defaults to ``16``.

Returns:
    str: Result produced by the operation.
```

## `lpw.utils.identifiers`

Source: [`src/lpw/utils/identifiers.py`](../src/lpw/utils/identifiers.py#L1)

```text
Provide identifiers services for the LPW cinematic pipeline.
```

### `validate_identifier`

Defined at line 13.

```python
def validate_identifier(value: str, field_name: str) -> str
```

```text
Validate identifier.

Args:
    value (str): Value inspected or transformed by the helper.
    field_name (str): Field name used by this operation.

Returns:
    str: Result produced by the operation.

Raises:
    InvalidContextIdentifierError: If inputs, context, state, or provider output are invalid.
```

## `lpw.utils.mappings`

Source: [`src/lpw/utils/mappings.py`](../src/lpw/utils/mappings.py#L1)

```text
Provide mappings services for the LPW cinematic pipeline.
```

### `deep_merge`

Defined at line 9.

```python
def deep_merge(base: dict[str, Any], override: dict[str, Any]) -> dict[str, Any]
```

```text
Execute merge.

Args:
    base (dict[str, Any]): Base used by this operation.
    override (dict[str, Any]): Override used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

## `lpw.utils.references`

Source: [`src/lpw/utils/references.py`](../src/lpw/utils/references.py#L1)

```text
Provide references services for the LPW cinematic pipeline.
```

### `collect_reference_images`

Defined at line 8.

```python
def collect_reference_images(value: Any) -> list[str]
```

```text
Recursively collect unique image references while preserving order.

Args:
    value (Any): Value inspected or transformed by the helper.

Returns:
    list[str]: Result produced by the operation.
```

## `lpw.validation`

Source: [`src/lpw/validation/__init__.py`](../src/lpw/validation/__init__.py#L1)

```text
Expose the public validation package API for LPW.
```

## `lpw.validation.finalization`

Source: [`src/lpw/validation/finalization.py`](../src/lpw/validation/finalization.py#L1)

```text
Provide finalization services for the LPW cinematic pipeline.
```

### `ValidationIssue`

Defined at line 20.

```python
class ValidationIssue
```

```text
Represent ValidationIssue behavior in the LPW pipeline.

Attributes:
    severity (str): Stored severity value.
    code (str): Stored code value.
    message (str): Stored message value.
```

### `FinalShotValidationReport`

Defined at line 34.

```python
class FinalShotValidationReport
```

```text
Represent FinalShotValidationReport behavior in the LPW pipeline.

Attributes:
    shot_id (str): Stored shot id value.
    valid (bool): Stored valid value.
    probe (dict[str, Any]): Stored probe value.
    issues (list[ValidationIssue]): Stored issues value.
```

### `run_process`

Defined at line 49.

```python
def run_process(arguments: list[str], *, check: bool = True) -> subprocess.CompletedProcess[str]
```

```text
Run process.

Args:
    arguments (list[str]): Command arguments passed without shell interpolation.
    check (bool): Check used by this operation. Defaults to ``True``.

Returns:
    subprocess.CompletedProcess[str]: Result produced by the operation.

Raises:
    RuntimeError: If inputs, context, state, or provider output are invalid.
```

### `probe_media`

Defined at line 82.

```python
def probe_media(path: Path) -> dict[str, Any]
```

```text
Execute media.

Args:
    path (Path): Filesystem path read or written by the operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    FileNotFoundError: If inputs, context, state, or provider output are invalid.
    RuntimeError: If inputs, context, state, or provider output are invalid.
```

### `parse_frame_rate`

Defined at line 115.

```python
def parse_frame_rate(value: str) -> float
```

```text
Parse frame rate.

Args:
    value (str): Value inspected or transformed by the helper.

Returns:
    float: Result produced by the operation.
```

### `find_stream`

Defined at line 131.

```python
def find_stream(probe: dict[str, Any], codec_type: str) -> dict[str, Any] | None
```

```text
Find stream.

Args:
    probe (dict[str, Any]): Probe used by this operation.
    codec_type (str): Codec type used by this operation.

Returns:
    dict[str, Any] | None: Result produced by the operation.
```

### `detect_black_and_frozen_segments`

Defined at line 153.

```python
def detect_black_and_frozen_segments(path: Path) -> tuple[list[float], list[float]]
```

```text
Detect black and frozen segments.

Args:
    path (Path): Filesystem path read or written by the operation.

Returns:
    tuple[list[float], list[float]]: Result produced by the operation.
```

### `validate_shot`

Defined at line 188.

```python
def validate_shot(*, shot_id: str, video_path: Path, video_rules: dict[str, Any], validation_rules: dict[str, Any]) -> FinalShotValidationReport
```

```text
Validate shot.

Args:
    shot_id (str): Stable identifier of the shot being processed.
    video_path (Path): Path to the video asset being processed.
    video_rules (dict[str, Any]): Video rules used by this operation.
    validation_rules (dict[str, Any]): Validation rules used by this operation.

Returns:
    FinalShotValidationReport: Result produced by the operation.
```

### `validate_approval`

Defined at line 295.

```python
def validate_approval(approval_path: Path, thresholds: dict[str, int]) -> list[ValidationIssue]
```

```text
Validate approval.

Args:
    approval_path (Path): Approval path used by this operation.
    thresholds (dict[str, int]): Thresholds used by this operation.

Returns:
    list[ValidationIssue]: Result produced by the operation.
```

### `write_validation_report`

Defined at line 361.

```python
def write_validation_report(report: FinalShotValidationReport, destination: Path) -> None
```

```text
Execute validation report.

Args:
    report (FinalShotValidationReport): Report used by this operation.
    destination (Path): Destination path for the transformed asset.
```

### `record_render_approval`

Defined at line 373.

```python
def record_render_approval(*, render_directory: Path | str, reviewer: str, scores: dict[str, int], continuity: dict[str, Any], review_notes: list[str] | None = None, project_root: Path | str = PROJECT_ROOT) -> dict[str, Any]
```

```text
Write one immutable creative approval beside a completed Wan render.

Args:
    render_directory (Path | str): Render directory used by this operation.
    reviewer (str): Human reviewer identity recorded with the decision.
    scores (dict[str, int]): Named review scores used by approval thresholds.
    continuity (dict[str, Any]): Continuity state approved for subsequent
        production.
    review_notes (list[str] | None): Structured or textual notes supplied by the
        reviewer. Defaults to ``None``.
    project_root (Path | str): Root directory containing project runtime data.
        Defaults to ``PROJECT_ROOT``.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

## `lpw.validation.media`

Source: [`src/lpw/validation/media.py`](../src/lpw/validation/media.py#L1)

```text
Provide media services for the LPW cinematic pipeline.
```

### `MediaValidator`

Defined at line 15.

```python
class MediaValidator
```

```text
Run deterministic FFprobe and FFmpeg checks for rendered media.
```

#### `MediaValidator.run_checks`

Defined at line 18.

```python
def run_checks(self, video_file: Path, project: dict[str, Any], shot: dict[str, Any], validation: dict[str, Any], rule_override: dict[str, Any] | None = None) -> list[dict[str, Any]]
```

```text
Run checks.

Args:
    video_file (Path): Path to the video file associated with the operation.
    project (dict[str, Any]): Project used by this operation.
    shot (dict[str, Any]): Shot used by this operation.
    validation (dict[str, Any]): Validation used by this operation.
    rule_override (dict[str, Any] | None): Rule override used by this operation.
        Defaults to ``None``.

Returns:
    list[dict[str, Any]]: Result produced by the operation.
```

#### `MediaValidator._execute`

Defined at line 141.

```python
def _execute(command: list[str]) -> subprocess.CompletedProcess[str]
```

```text
Execute execute.

Args:
    command (list[str]): Command used by this operation.

Returns:
    subprocess.CompletedProcess[str]: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `MediaValidator._probe_media`

Defined at line 160.

```python
def _probe_media(self, media_file: Path) -> dict[str, Any]
```

```text
Execute media.

Args:
    media_file (Path): Media file used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `MediaValidator._check_corruption`

Defined at line 187.

```python
def _check_corruption(self, video_file: Path) -> dict[str, Any]
```

```text
Execute corruption.

Args:
    video_file (Path): Path to the video file associated with the operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `MediaValidator._check_black_frames`

Defined at line 200.

```python
def _check_black_frames(self, video_file: Path, rules: dict[str, Any]) -> dict[str, Any]
```

```text
Execute black frames.

Args:
    video_file (Path): Path to the video file associated with the operation.
    rules (dict[str, Any]): Rules used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `MediaValidator._check_audio_peak`

Defined at line 227.

```python
def _check_audio_peak(self, video_file: Path, rules: dict[str, Any]) -> dict[str, Any]
```

```text
Execute audio peak.

Args:
    video_file (Path): Path to the video file associated with the operation.
    rules (dict[str, Any]): Rules used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `MediaValidator._check`

Defined at line 249.

```python
def _check(check_id: str, passed: bool, expected: Any, actual: Any) -> dict[str, Any]
```

```text
Execute check.

Args:
    check_id (str): Check id used by this operation.
    passed (bool): Passed used by this operation.
    expected (Any): Expected used by this operation.
    actual (Any): Actual used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `MediaValidator._first_stream`

Defined at line 264.

```python
def _first_stream(probe: dict[str, Any], codec_type: str) -> dict[str, Any] | None
```

```text
Execute stream.

Args:
    probe (dict[str, Any]): Probe used by this operation.
    codec_type (str): Codec type used by this operation.

Returns:
    dict[str, Any] | None: Result produced by the operation.
```

#### `MediaValidator._media_duration`

Defined at line 277.

```python
def _media_duration(probe: dict[str, Any], video_stream: dict[str, Any]) -> float
```

```text
Execute duration.

Args:
    probe (dict[str, Any]): Probe used by this operation.
    video_stream (dict[str, Any]): Video stream used by this operation.

Returns:
    float: Result produced by the operation.
```

#### `MediaValidator._frame_count`

Defined at line 293.

```python
def _frame_count(video_stream: dict[str, Any], duration: float, fps: float) -> int
```

```text
Execute count.

Args:
    video_stream (dict[str, Any]): Video stream used by this operation.
    duration (float): Duration used by this operation.
    fps (float): Fps used by this operation.

Returns:
    int: Result produced by the operation.
```

#### `MediaValidator._parse_fraction`

Defined at line 311.

```python
def _parse_fraction(value: str) -> float
```

```text
Parse fraction.

Args:
    value (str): Value inspected or transformed by the helper.

Returns:
    float: Result produced by the operation.
```

## `lpw.validation.pipeline`

Source: [`src/lpw/validation/pipeline.py`](../src/lpw/validation/pipeline.py#L1)

```text
Provide pipeline services for the LPW cinematic pipeline.
```

### `RenderValidationPipeline`

Defined at line 33.

```python
class RenderValidationPipeline
```

```text
Store, validate, review, and approve versioned render artifacts.
```

#### `RenderValidationPipeline.__init__`

Defined at line 36.

```python
def __init__(self, project_root: Path | str, context_root: Path | str = DEFAULT_CONTEXT_ROOT, renders_root: Path | str | None = None, media_validator: MediaValidator | None = None) -> None
```

```text
Initialize the service with its configured dependencies.

Args:
    project_root (Path | str): Root directory containing project runtime data.
    context_root (Path | str): Root directory containing source context files.
        Defaults to ``DEFAULT_CONTEXT_ROOT``.
    renders_root (Path | str | None): Renders root used by this operation.
        Defaults to ``None``.
    media_validator (MediaValidator | None): Media validator used by this
        operation. Defaults to ``None``.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `RenderValidationPipeline.create_attempt`

Defined at line 71.

```python
def create_attempt(self, story_id: str, scene_id: str, shot_id: str, video_file: Path | str, dialogue_file: Path | str | None = None) -> dict[str, Any]
```

```text
Create attempt.

Args:
    story_id (str): Stable identifier of the story to load or compile.
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.
    video_file (Path | str): Path to the video file associated with the
        operation.
    dialogue_file (Path | str | None): Dialogue file used by this operation.
        Defaults to ``None``.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `RenderValidationPipeline.validate_attempt`

Defined at line 184.

```python
def validate_attempt(self, attempt_directory: Path | str) -> dict[str, Any]
```

```text
Validate attempt.

Args:
    attempt_directory (Path | str): Attempt directory used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.
```

#### `RenderValidationPipeline.record_semantic_review`

Defined at line 234.

```python
def record_semantic_review(self, attempt_directory: Path | str, reviewer: str, results: dict[str, dict[str, str]]) -> dict[str, Any]
```

```text
Record semantic review.

Args:
    attempt_directory (Path | str): Attempt directory used by this operation.
    reviewer (str): Human reviewer identity recorded with the decision.
    results (dict[str, dict[str, str]]): Results used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `RenderValidationPipeline.approve_attempt`

Defined at line 293.

```python
def approve_attempt(self, attempt_directory: Path | str, reviewer: str, notes: str = '', scores: dict[str, int] | None = None, continuity: dict[str, Any] | None = None) -> dict[str, Any]
```

```text
Approve attempt.

Args:
    attempt_directory (Path | str): Attempt directory used by this operation.
    reviewer (str): Human reviewer identity recorded with the decision.
    notes (str): Optional review or production notes. Defaults to ``''``.
    scores (dict[str, int] | None): Named review scores used by approval
        thresholds. Defaults to ``None``.
    continuity (dict[str, Any] | None): Continuity state approved for subsequent
        production. Defaults to ``None``.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `RenderValidationPipeline.reject_attempt`

Defined at line 375.

```python
def reject_attempt(self, attempt_directory: Path | str, reviewer: str, reason: str) -> dict[str, Any]
```

```text
Reject attempt.

Args:
    attempt_directory (Path | str): Attempt directory used by this operation.
    reviewer (str): Human reviewer identity recorded with the decision.
    reason (str): Reason used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `RenderValidationPipeline.create_scene_release`

Defined at line 428.

```python
def create_scene_release(self, story_id: str, scene_id: str, final_video: Path | str) -> dict[str, Any]
```

```text
Create scene release.

Args:
    story_id (str): Stable identifier of the story to load or compile.
    scene_id (str): Stable identifier of the scene being processed.
    final_video (Path | str): Final video used by this operation.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `RenderValidationPipeline.get_approved_scene_attempts`

Defined at line 518.

```python
def get_approved_scene_attempts(self, story_id: str, scene_id: str) -> list[dict[str, Any]]
```

```text
Return the latest integrity-checked approval for every scene shot.

Args:
    story_id (str): Stable identifier of the story to load or compile.
    scene_id (str): Stable identifier of the scene being processed.

Returns:
    list[dict[str, Any]]: Result produced by the operation.
```

#### `RenderValidationPipeline._find_approved_scene_attempts`

Defined at line 534.

```python
def _find_approved_scene_attempts(self, scene: dict[str, Any]) -> list[dict[str, Any]]
```

```text
Find approved scene attempts.

Args:
    scene (dict[str, Any]): Scene used by this operation.

Returns:
    list[dict[str, Any]]: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `RenderValidationPipeline._latest_approved_attempt`

Defined at line 558.

```python
def _latest_approved_attempt(self, scene_id: str, shot_id: str) -> dict[str, Any] | None
```

```text
Execute approved attempt.

Args:
    scene_id (str): Stable identifier of the scene being processed.
    shot_id (str): Stable identifier of the shot being processed.

Returns:
    dict[str, Any] | None: Result produced by the operation.
```

#### `RenderValidationPipeline._verify_attempt_integrity`

Defined at line 595.

```python
def _verify_attempt_integrity(self, attempt_path: Path) -> None
```

```text
Execute attempt integrity.

Args:
    attempt_path (Path): Attempt path used by this operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `RenderValidationPipeline._verify_embedded_hash`

Defined at line 640.

```python
def _verify_embedded_hash(cls, value: dict[str, Any], field: str, label: str) -> None
```

```text
Execute embedded hash.

Args:
    value (dict[str, Any]): Value inspected or transformed by the helper.
    field (str): Field used by this operation.
    label (str): Label used by this operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `RenderValidationPipeline._build_semantic_checks`

Defined at line 659.

```python
def _build_semantic_checks(shot_id: str, validation: dict[str, Any]) -> list[dict[str, Any]]
```

```text
Build semantic checks.

Args:
    shot_id (str): Stable identifier of the shot being processed.
    validation (dict[str, Any]): Validation used by this operation.

Returns:
    list[dict[str, Any]]: Result produced by the operation.
```

#### `RenderValidationPipeline._assert_approvable`

Defined at line 692.

```python
def _assert_approvable(report: dict[str, Any], rules: dict[str, Any]) -> None
```

```text
Execute approvable.

Args:
    report (dict[str, Any]): Report used by this operation.
    rules (dict[str, Any]): Rules used by this operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `RenderValidationPipeline._resolve_attempt`

Defined at line 725.

```python
def _resolve_attempt(self, value: Path | str) -> Path
```

```text
Resolve attempt.

Args:
    value (Path | str): Value inspected or transformed by the helper.

Returns:
    Path: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `RenderValidationPipeline._validate_scores`

Defined at line 749.

```python
def _validate_scores(scores: dict[str, int]) -> dict[str, int]
```

```text
Validate scores.

Args:
    scores (dict[str, int]): Named review scores used by approval thresholds.

Returns:
    dict[str, int]: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `RenderValidationPipeline._next_number`

Defined at line 777.

```python
def _next_number(directory: Path, prefix: str) -> int
```

```text
Execute number.

Args:
    directory (Path): Directory used by this operation.
    prefix (str): Prefix used by this operation.

Returns:
    int: Result produced by the operation.
```

#### `RenderValidationPipeline._find_shot`

Defined at line 799.

```python
def _find_shot(scene: dict[str, Any], shot_id: str) -> dict[str, Any]
```

```text
Find shot.

Args:
    scene (dict[str, Any]): Scene used by this operation.
    shot_id (str): Stable identifier of the shot being processed.

Returns:
    dict[str, Any]: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `RenderValidationPipeline._require_file`

Defined at line 820.

```python
def _require_file(value: Path | str, label: str) -> Path
```

```text
Execute file.

Args:
    value (Path | str): Value inspected or transformed by the helper.
    label (str): Label used by this operation.

Returns:
    Path: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `RenderValidationPipeline._require_text`

Defined at line 839.

```python
def _require_text(value: str, label: str) -> str
```

```text
Execute text.

Args:
    value (str): Value inspected or transformed by the helper.
    label (str): Label used by this operation.

Returns:
    str: Result produced by the operation.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `RenderValidationPipeline._write_new_json`

Defined at line 857.

```python
def _write_new_json(path: Path, value: Any) -> None
```

```text
Execute new json.

Args:
    path (Path): Filesystem path read or written by the operation.
    value (Any): Value inspected or transformed by the helper.

Raises:
    GenerationPipelineError: If inputs, context, state, or provider output are invalid.
```

#### `RenderValidationPipeline._hash_json`

Defined at line 872.

```python
def _hash_json(value: Any) -> str
```

```text
Execute json.

Args:
    value (Any): Value inspected or transformed by the helper.

Returns:
    str: Result produced by the operation.
```

#### `RenderValidationPipeline._hash_without_field`

Defined at line 887.

```python
def _hash_without_field(cls, value: dict[str, Any], field: str) -> str
```

```text
Execute without field.

Args:
    value (dict[str, Any]): Value inspected or transformed by the helper.
    field (str): Field used by this operation.

Returns:
    str: Result produced by the operation.
```

#### `RenderValidationPipeline._hash_file`

Defined at line 900.

```python
def _hash_file(path: Path) -> str
```

```text
Execute file.

Args:
    path (Path): Filesystem path read or written by the operation.

Returns:
    str: Result produced by the operation.
```

#### `RenderValidationPipeline._now_iso`

Defined at line 916.

```python
def _now_iso() -> str
```

```text
Execute iso.

Returns:
    str: Result produced by the operation.
```
